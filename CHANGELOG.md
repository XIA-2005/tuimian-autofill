# 更新日志

## v2.0.2（弹窗人工接管、高亮完善与填写流程加固）

- 弹窗自动选择无法可靠完成时，新增人工接管卡片，支持回填后继续、跳过当前项、自动检测回填与完整生命周期清理；
- 加固弹窗队列与顶层页面任务隔离，避免延时补填重复打开选择器、子框架重复执行动态表任务或关键字写入错误弹窗；
- 补齐漏填高亮 UI 模块和三色样式映射，高亮状态互斥、可重复清理，并避免未参与本轮填写的无关控件被误标；
- 恢复统一的“一键填充”面板文案并新增回归测试；完整类型检查与自动化测试通过。

## v2.0.0（统一档案 V2、八类原子表、声明式适配包与安全爬取会话）

- 档案升级为八类原子表，新增语言考试、计算机证书、长文、代码簿、学校扩展字段、来源与稳定行 ID；V1 无损迁移，不明确记录进入待分类并保留旧版备份；
- 新增标量、单行、整表锁定和冲突合并预览；手工有效值自动锁定，爬取与迁移不覆盖锁定内容；排名/总人数按字段组锁定；
- 学校目录拆分学校与项目分支，南理工夏令营/预推免、浙大夏令营/预推免等分别记录入口和状态；
- 新增声明式学校适配包、页面门禁、页面结构指纹、代码/名称成对写入与即时回读、八表学校投影；首批学校均保持“实验性”，真实页面验收前不标记已验证；
- 新增当前页累计爬取和登录后只读会话爬取，支持预览、取消、超时、限速、冲突保留与一键锁定；北邮多条报名记录必须由用户确认；
- 规则热更新改为候选校验、原子切换、上一版回滚；远程规则仅允许 JSON 声明，不接受函数或远程执行代码；
- 新增用户主动开启的“连续填写（自动下一步）”：只在适配包声明且已验收的表单页执行，点击前自动补填、严格回读代码/名称与日期、检查必填和服务器错误；单页纠错最多两轮、下一步最多三次、全程最多二十步；登录、上传、汇总、锁定和最终提交页自动停止；
- 普通“一键填充”仍只处理当前页，不自动点击保存、下一步、行内落库型“添加”或最终提交；动态表格可安全点击“新增一行”，确认新行出现后继续填写；字段报告全面脱敏；
- 本科院校/本科专业弹窗改为当前字段精确绑定代码框与名称框，支持隐藏码值、新旧专业代码别名和“目标代码→标准代码→名称”查询顺序；码名任一缺失均不再判定成功，也不会向同表单其他 `dm/mc` 字段兜底写值；
- 新增原生 date/month、My97、Laydate、Ant、Element、jQuery UI、Bootstrap 日期组件识别，按年/月/日精度格式化，写入后校验错误状态，并在组件失焦异步重置时恢复档案日期；
- 院校、专业、日期拆分为三个独立驱动内核；简约系统新增 `SelUniversity` / `SelBkdzZydm` 专项 iframe 查询、专业门类和回读流程；新增 Ant、Select2、Element、Layui 专用选项驱动，分别校验可见标签、选中态、原生 select 或选项代码；
- 日期内核增加真实日历面板翻月/点选、确定/关闭，以及适配包声明的隐藏模型完整回读；学校适配包可声明组件类型、触发器、frame、代码/名称、日期模型和面板选择器；
- 新增北京科技大学夏令营蓝色系统专项契约：`bydwm/bydw/bkbydwShow` 与 `byzydm/byzymc/bkbyzyShow` 按代码、名称、展示三联填写；支持 `chooseSch/chooseZy` 同源弹窗、`span/td onclick` 结果行、标准院校/专业名称反查代码及完整三联回读，修复所在学校和“测控技术与仪器”只搜索不回填的问题；
- 北科大外语水平页新增选项型等级驱动：支持四级、六级、托福、雅思及“其它”，等级、成绩、日期按同一语言考试原子记录成对填写；未知考试写入“其它”并在备注保留真实类型，多条考试可安全新增行；
- 自动填写提示升级为大尺寸阶段横幅和加宽面板，持续显示百分比、当前处理阶段、数量、勿刷新/勿重复点击提醒、自动回读纠错状态及最终提交安全边界；
- 新增 V2 jsdom 回归与 Playwright 本地 Manifest V3 扩展测试。
- 修复合工大教育步骤院校/专业弹窗无法打开的问题：蓝色三联驱动定位弹层时，扩展自身面板（运行日志文案含"选择"且命中 [class*="panel"] 弹层选择器）被误判成"已打开的选择器弹层"，导致触发按钮永远不会被点击、诊断停留在 scope-found→exhausted；现在扩展 UI 一律排除出弹层候选，且弹窗已具备"关键字+查询"能力又无级联下拉/级联标签时直接走搜索点选路径，不再空转逐级点击烧掉时间预算。
- 🐛 **修复广工大第二轮组件下拉仍未填写**：真实 jqx 选项会同时出现在“组件容器 + 已展开列表”中，且性别值域可能是没有 name 的显示输入框。此前组件候选扫描被错误的 name 守卫跳过，导致政治面貌/性别看似已识别但点击不到；现改为仅对带 name 的真实表单控件让常规检测接管，无 name 显示框统一走组件点选；候选浮层支持 jqx-item，组件回读优先按 data-tui key 找到显示载体并以 VALUE_ALIASES 兼容 Male/Female。
- 🐛 **组件下拉第二轮加固（广工大实测反馈）**：jqx 会把选项列表（甚至英文，Male/Female）预渲染在容器里污染标签匹配 → 标签提取前先剥离选项列表元素，剥离后无标签则取前一格；jqx 的显示输入框（无 name）被认领为值载体并从常规检测排除（不再产生 noMatch 噪音，也不会被原生写入文本破坏组件模型），带 name 的真实字段仍走常规填写；点选与回读全部按“档案值 + 编码别名”（男→male/m/1，VALUE_ALIASES）匹配，英文选项页面可正确点中；
- 🐛 **修复组件下拉字段（性别/政治面貌等）永远填不上**（广东工业大学 ehall gsapp 实测）：这类页面用 jqx/自定义组件把下拉渲染成“请选择...”span + 隐藏域，页面上没有任何原生 select，常规检测完全看不见。现在新增组件下拉通用识别与点选内核：检测可见“请选择”占位组件 + 容器标签命中规则（容器内无原生 select 才介入，只取最内层组件元素）；点选时点开组件 → 轮询浮层选项（复用通用选项匹配，含同源 iframe）→ 逐个尝试点选 → 按组件文本/隐藏域回读验证，幂等跳过已选组件，失败自动收起浮层并给出醒目人工提示；
- 🐛 **修复填写完成后“一键填充”永久禁用**（广东工业大学 ehall 页实测）：填写结束后的迟到日志事件（15 秒补填轮次重跑、子框架回报）会把面板阶段翻回“进行中”，而按钮可用性此前按阶段推断 → 按钮卡死在禁用态。现在按钮禁用改为显式控制：只在填写/爬取真正运行时禁用，完成或失败立即可再次点击；面板同时移除残留的“下一步”阶段图标；
- 移除“连续填写（自动下一步）”：每页由用户手动进入下一步，扩展专注于把当前页填好、校验好；面板精简为单一主操作「⚡ 一键填充（含自动加行与弹窗点选）」；
- 新增顶部醒目 toast 通知：需要人工介入的关键事件（弹窗字段待手动选择、下拉/单选未自动选中、长文未填、疑似假保存、清除本页已填结果）不再只躺在面板日志里；
- 缩短弹窗等待：弹窗点选总预算 30s → 20s；选择器“成对回读”兜底由固定空等 7 秒收紧为 2.5 秒无回填立即明文写入；新增弹窗确认成功后若未自动关闭立即温和关闭，避免遮挡后续字段；
- 五类动态表（学术成果/学习工作经历/奖励情况/外语水平/家庭成员）的自动加行循环收敛为统一填充内核 `src/core/dynamic-table.ts`：安全骨架（验证式加行、连续失败计数封顶、行数上限停止、编辑弹窗误开防护、整页回发刹车、断点推进）只写一处，每表差异（去重键、空行判定、行填写、行内落库、弹窗填写）改为配置声明；经历/奖励/家庭/外语表同步补齐旧文档刹车与回发等待，外语表明确"绝不点保存"；成果新增弹窗复用时上一条残留的字段值改为按语义直接覆盖（避免第二条记录沿用第一条的刊物/时间）。
- 新增隐藏行串同步内核 `src/core/hidden-blob.ts`（川大/长大式"可见子表+隐藏行码串"）：统一 `|` 连格 `#` 连行的编解码，单元格连接符自动替换为全角、换行压空格；超过软上限时二分缩格保行（绝不静默丢行）；动态表每条写入后按页面实况回读重编码同步关联隐藏域（同 form、已呈行串形状、列数匹配才关联，空隐藏域无证据绝不猜写）；隐藏串比实况更富时保留原值作回发丢行证据；实况行按 host 落盘跨页 stash（富者优先），供断点续填核对。
- 新增问题码体系 `src/core/error-codes.ts`（E1101 未识别规则/E1102 档案缺失/E1103 选项不匹配/E1201 加行无效/E1202 行数上限/E1203 弹窗需人工/E1204 编辑弹窗安全关闭/E1205 疑似假保存/E1206 长文未填/E1207 无加行按钮，每个码附"用户该做什么"）：FillItem 携带 issueCode，漏填清单分组给建议，字段报告携带问题码目录与逐项 issue。
- 长文（个人陈述/自述/研究计划等）改为按页面证据填写：页面给出 maxlength 或"不超过/最多 N 字"文案、且档案长文放得下时自动填写（绝不静默截断）；超限或无匹配长文时跳过并给出 E1206 与具体原因；备注/推荐人等非长文语义字段绝不拿个人陈述顶上。
- 面板新增「清除本页已填」：只清空本扩展标记为 filled 的控件值（用户手填/半填内容不受影响，密码/验证码/未标记字段永不触碰），清除后停止自动补填防回写。

## v1.15.4（苏州大学：入学/毕业年月被日历重置成一样 → 自愈恢复）

- 🐛 **修复只读日历框"入学=毕业"事故**：入学年月/预计毕业年月（My97 日历只读框）填充后，我们为同步页面状态会模拟"点开-收起"日历，而 My97 一点开就把值重置成**当前月份**，两个框全变成同一个"今天"。现在填充后做自愈校验：日历把值改掉（年月与档案不一致）就自动恢复档案值；同一日期仅是格式不同的（如 2021年9月）保留页面格式；
- 外语水平表的取得时间（只读日历）同样套用自愈；
- 🧪 新增自愈回归断言（模拟日历点开即重置当前月份）。

## v1.15.3（苏州大学：奖励表无删行按钮也识别）

- 🐛 **修复苏大奖励情况无法填充**：苏大（与东华同系统的蓝表）奖励网格行内**没有「删除」按钮**，旧规则要求"无关键词网格必须有加行/删行按钮"→ 匹配失败。现在：页面存在「何时何地何原因受过何种奖励」等**奖励标题表**时，「时间/地点/内容」网格即使无任何按钮也认定为奖励表（巨能填 titleFirst 同款思路）；同步填充与自动加行两条路径都已覆盖；
- 🧪 新增苏大式奖励表断言（无按钮网格 + 外层标题表）。

## v1.15.2（获奖情况档案结构改为 时间/地点/内容）

- 🗂️ **获奖情况字段结构变更**：档案编辑器与数据模型中「获奖情况」由（奖项名称/级别/时间/排名）改为 **时间 / 地点 / 内容**；表格填充按 时间→时间列、地点→单位/地点列、内容→名称/内容列 逐行写入；长文本自动合成格式变为 `时间，地点，内容`；
- 🔄 **旧档案自动迁移**：旧格式 `{name, level, date, role}` 载入时自动转换（`学校·奖项` 前缀拆成地点+内容；无前缀时地点回退为毕业院校；级别/排名保留用于南理工式「奖励原因」列），用户无需重填；
- 📥 反向提取同步按 时间/地点/内容 入库；
- 🧪 新增旧档案迁移断言，更新全部奖励相关断言。

## v1.15.1（反向提取增强：单选/复选、四六级按名称归类、同源 iframe）

- 📥 **从本页提取档案**大幅完善：
  - 单选/复选组按**选中项**导入（如 性别=女、服从调剂=是），此前整组跳过；
  - 外语水平表按行内名称匹配四级/六级（六级在前也不串位），此前按行号盲取；
  - 扫描范围扩展到**同源 iframe**（主表单嵌在框架里的站点也能提取）；
  - 学术成果类型按内容推导（竞赛/大赛→竞赛，刊物列→论文）；
  - 组标签识别修正：只取本组第一个控件之前的文本，杜绝跨组串台（单选组误用其它组标题）；
- 🧪 新增 4 组反向提取断言。

## v1.15.0（精简：移除「站点向导」与「引导填写待选项」）

- 🧹 按用户要求移除两个功能：「🧭 站点向导（整站逐页填充）」与「🎯 引导填写待选项」（按钮、逻辑、核心模块、样式与测试全部删除）。弹窗选择框仍由「一键填充」自动点选处理，剩余需人工项请用「📋 复制漏填项」+ 红色高亮核对；
- 面板更简洁：一键填充 / 学校目录 / 提交前体检 / 提取档案 / 复制漏填项 / 字段报告 / 清除高亮。

## v1.14.18（全面审查：消灭"覆盖用户数据"类严重隐患）

- 🛡️ **整行全空才填**：经历/成果/奖励/家庭成员四类网格的"空行"判定从"关键格为空"收紧为"整行所有可写格为空"——用户手填了一半的行（如只填了时间）绝不被覆盖，改填下一个全空行；
- 🛡️ **外语水平表不覆盖、不重复**：行内已有内容且与档案不一致时保留原样；已有"六级"行时四级写入空行（按内容匹配，不再按行号盲填导致重复行）；
- 🛡️ **选择器"代码+名称"配对排除邮编/电话/邮箱类字段**（yzbm/postcode/phone/sjh/…）：兜底直写时不再把行码写进邮编框、把值写进电话框；
- 🧪 新增 3 组回归断言：半填行不覆盖 / 外语水平表不覆盖不重复 / 选择器不污染主表单。

## v1.14.17（海大：修复"姓名被写成共青团员" + 弹窗点选提速与超时中止）

- 🐛 **修复主表单被污染**：选择器关键字路径此前会把关键字打进主页面任意空输入框（海大事故：把「共青团员」打进了姓名框）。现在：
  - 弹层上下文若是主页面/主表单（或含目标字段）→ 找不到「查询/搜索」按钮就直接放弃关键字路径，**绝不向主表单输入框打字**；
  - 已识别为档案字段的输入框一律不充当弹层搜索框；
- ⚡ **点选提速**：打开弹层改为「打开即停」（el-select 是 toggle，连续策略会把刚打开的下拉再点关）；弹层检测识别 el-popper；弹层点选整体超时 45 秒 → 30 秒，且**超时后内部流程立即中止**（不再继续打字/点击）；页面级误判时跳过末尾 8×600ms 空等扫描；
- 🧪 新增回归断言：选择器失败时绝不向主表单输入框打字。

## v1.14.16（中国海洋大学 Element-UI：下拉点选升级 + 外语水平合成 + 单选组标签拼接）

- 🎯 **el-select 下拉点选升级**（巨能填同款三连）：选项异步渲染 → 轮询等待（每 300ms，最多 ~4 秒）；点选改派发 `mousedown→mouseup→click` 三连；点完校验回填，未回填自动换下一候选并重新点开下拉；无独立「选择」按钮的 el-select（海大式）直接点输入框自身展开下拉；
- ✍️ 新增「外语水平」单输入框规则：自动合成 `大学英语四级（CET-4）xx分；大学英语六级（CET-6）xx分` 摘要（负向排除 CET 表格列，避免与网格填充冲突）；
- 🔀 单选组标签拼接回退：标签被拆成片段（如「是否」+「服从调剂」两个 span）时自动拼接还原完整问句，命中「服从调剂」规则并选中「是」；单选值别名补 `true/false`；
- 🧪 新增 2 组断言：外语水平摘要合成 / 单选组片段拼接选中。

## v1.14.15（中南大学：电子信箱识别）

- 🐛 邮箱字段补别名：「电子信箱」（中南大学标签）以及 `email`/`dzxx` 属性线索现在都能命中邮箱规则，一键填充写入档案邮箱；
- 🧪 新增「电子信箱」标签回归断言。

## v1.14.14（首点必中：网格未渲染完时等待，不再"前几次点了没反应"）

- 🐛 **修复"前两次点击没填写、第三次才成功"**：东华表格是页面脚本异步渲染的，此前行任务开头的「等网格稳定」只探测经历/成果两类表（漏了奖励/家庭），且页面尚无网格时立即放弃任务。现在：
  - 四类网格（经历/成果/奖励/家庭）全部参与稳定探测，表已出现且行数连续两次一致才开填；
  - 页面还没有网格时最多等 3 秒再放弃（覆盖"刚进页面就点填充"的场景）；
  - 每次点「一键填充」先清除过期的回发信号，上一页导航的时间戳不再误拦本轮点击 8 秒；
  - 任务因"本页无该表"被放弃时写入 `dropped-no-table-on-page` 诊断记录。

## v1.14.13（东华奖励情况：同型问题一次根治——表头无关键词的网格统一识别）

- 🐛 **修复奖励表漏识别**（与学术成果同型）：东华奖励网格表头是「时间/地点/内容」，「奖励」字样只在外层标题表里 → 旧规则要求网格表头含「奖励/荣誉/获奖」而匹配失败。现在奖励表与成果表统一判定：**有关键词 OR（列像奖励表 + 有加行/删行按钮）**；并统一多候选打分（可写空行 > 加删按钮 > 控件数，含嵌套表格的布局包装表巨幅降权）；
- 🛡️ 成果表、经历表打分同步加入「嵌套包装表降权」，同类问题防复发；
- 🧪 新增 3 组断言：东华式奖励表识别+填写+加行 / 外层包装表不误选 / 无加删按钮的列表负例。

## v1.14.12（自动加行提速：固定等待 2.5 秒 → 短轮询即时换行）

- ⚡ **加行提速**：点「新增一行/保存」后不再固定等 2.5 秒，改为每 120ms 短轮询行数（巨能填 `ensureBlueTableRowCount` 轮询同款）：东华等客户端 JS 加行**即时生效**（每行约 0.2 秒，14 行从约 35 秒缩到约 5 秒）；行数 3 秒内未增长才停止连点；整页回发页面仍由断点续填接管，行为不变；
- ⚡ 北邮式「行内添加」回发后的交接等待 2.5 秒 → 1.5 秒。

## v1.14.11（东华学术成果：嵌套布局表——加行按钮在外层包装表里）

- 🐛 **修复嵌套布局下找不到加行按钮**：`addbtnDebug` 证实真按钮 `#addNewRow` 未被禁用，但嵌套在东华的外层包装表里（网格表也嵌在其中），被"排除其他表格按钮"规则误伤。现在只排除**无关表**里的按钮，**祖先表**（包装网格的外层表）里的按钮是合法加行按钮（巨能填全局兜底同款思路）；
- 🧪 新增嵌套布局回归断言：网格+按钮同处外层表时正常加行。

## v1.14.10（东华学术成果：真凶抓到——一直在点自己横幅）

- 🐛 **修复"点了没反应"的真凶**：`clickDebug` 证实此前点的是扩展自己的进度横幅 `tui-banner-title`（「🔄 正在自动**加行**」撞上加行按钮关键词），页面真正的「新增一行」按钮从未被点击。现在：
  - 查找页面按钮时**排除扩展自身 UI**（面板/横幅/向导提示/检查报告/学校目录）；
  - 加行按钮关键词收紧为「新增/添加/增加/插入（一行）」等明确措辞，不再匹配"自动加行"这类进度文案；
  - 被跳过的候选（disabled/其他表格/自身 UI）写入 `tui-addbtn-debug` 诊断；字段报告的按钮列表新增 `disabled`/`onclick` 字段；
- 🧪 新增回归断言：含"添加"字样的横幅 span 不被误当加行按钮，真按钮正常加行。

## v1.14.9（东华学术成果"新增一行"点不动：多策略触发 + 点击诊断）

- 🖱️ **加行/保存按钮多策略触发**（巨能填经验：真站按钮的处理器常在"绑定脚本"里，普通 `.click()` 不触发）：先派发完整事件序列 `pointerdown→mousedown→focus→pointerup→mouseup→click`（兼容绑定在 mousedown/pointerdown 上的处理器），再用主世界 `jQuery(el).trigger('click')` 兜底（直达 jQuery 绑定的处理器）；
- 🚫 查找加行按钮时跳过 `disabled` 按钮（点了也没反应，还会误报进度）；
- 🎯 `findAddButton` 多候选打分：真实 `button/a/input` > 装饰性 `span/div`，带内联 `onclick` 或 class 含 `add` 者加分——避免点到无处理器的文本节点；
- 🔍 新增 `tui-click-debug` 点击诊断（被点元素 tag/id/class/disabled/onclick + 触发策略），已并入字段报告 `clickDebug` 键；
- 🧪 新增 2 组断言：mousedown 绑定式加行处理器触发 / 禁用按钮跳过。

## v1.14.8（东华学术成果：网格表头只有"标题"也识别；四类表格统一加行增长验证）

- 🐛 **修复学术成果表漏识别**：东华成果网格表头是「时间/发表刊物或出版社/标题/作者排名」，没有"成果/论文/专著"字样（它们只在外层标题表里），旧规则要求"标题+其他列含成果词"两个条件同时成立 → 匹配失败、任务静默放弃。现在"标题"列只要伴随 **刊物/出版** 或 **排名** 列即认定为成果表（巨能填 `fblwzz` 表头组合判断同款思路）；纯"标题+时间"的通知列表仍不算（防误吞）；
- 🛡️ **四类行任务统一加行增长验证**：学术成果/奖励情况/家庭成员 与学习工作经历一致——点「新增一行/保存」后核对行数是否真的增长，没增长立即停止连点；
- 🧪 新增 3 组断言：东华式成果表识别与填写（含刊物前缀剥离）/ 外层标题表不误选 / 标题+时间通知列表负例。

## v1.14.7（吸收巨能填多行表格三重门禁：通用防"无限新增一行"）

- 🛡️ **表定位结构否决**（巨能填 `tableLooksLikeBlueDynamicTable` 同款）：行内控件带 `rxny/byny/gpa/byyxmc` 等标量学习信息字段的表，或完全没有输入控件的包装大表，一律不当成经历网格——从根上杜绝"误把别的表当经历表"；
- 🛡️ **加行增长验证**（巨能填 `ensureBlueTableRowCount` 同款）：点「新增一行/保存」后核对行数是否真的增长，没增长就立刻停手不再连点，杜绝"一直加行不写数"；
- ✍️ **缺「职务」列时职务并入单位格**（巨能填 `adaptBlueTableCells` 列数适配）：页面只有 时间+单位 两列时不再丢失职务信息；
- 🧪 新增 4 组断言：标量表否决 / 包装大表不压真网格 / 加行无效只点一次 / 职务并入单位。

## v1.14.6（东华学习和工作经历：识别分列表头，不再误点"新增一行"）

- 🐛 修复经历表误识别：东华经历表是"起始时间/结束时间**分列** + 日期格式 2019-11"，此前因表头没有"学习或工作/起止时间"字样而漏识别，把只有 1 行的**外层包装大表**当成了经历表，于是反复点它的「新增一行」导致只加行不写数——现在支持分列表头识别，且时间按 `2019-11` 格式**分列写入**（起/止各一列）；
- 🧪 新增东华式经历表测试断言（含包装大表干扰场景）。

## v1.14.5（东华学习信息：学校/专业代码树弹层）

- 🌳 **代码/名称树弹层支持**（巨能填 `_pick_region_tree` 同款套路）：东华 `treeSelectPage?lbcode=getZY/getSchool` 这类"门类→专业类→专业"代码树（节点如 `080301 测控技术与仪器`）——检测到树形弹层后走"**关键字搜索 → 点击匹配节点 → 点「确定」**"，不再空等永远不存在的表格行；搜索没露出叶子时先点门类节点（如"工学"）展开再搜一次；
- 📅 入学/毕业年月（只读+日历控件）：写成 `YYYYMM` 紧凑格式，避免站点日期控件把值重置成"今天"（此前 rxny/byny 出现 202608 这类错值）；
- 🧪 新增代码树弹层点选测试断言。

## v1.14.4（页面载入即重置失败计数 + 查询按钮主世界执行）

- 🔁 **页面载入即重置弹窗失败计数**（成熟填表软件同款：失败防护不跨会话）——只要刷新页面，选择器就获得全新机会，旧版本失败不会再把修复后的选择器"拉黑"（南理工本科学校之前正是被旧失败计数跳过，连重试都没发生）；
- 🎯 **查询按钮主世界执行**（巨能填 `clickMinimalTriggerInPageWorld` 同款）：`javascript:__doPostBack(...)` 型「查询」链接改在弹层 iframe 的页面主世界执行回发，隔离世界 click 不稳定的问题彻底绕开。

## v1.14.3（重置弹窗失败计数：新版本不再被旧失败"拉黑"）

- 🐛 修复"选择器失败 3 次后本字段被永久跳过"的隐患：失败计数存在标签页 sessionStorage 里，上一轮失败会让新版修复后的选择器**根本没机会重试**（南理工本科学校就是这么被跳过的）——现在每次用户主动点「一键填充」都会重置失败/轮次计数，每轮都有全新 3 次尝试机会。

## v1.14.2（南理工学校选择器：查询回发间隙不再放弃）

- 🐛 修复"查询后 iframe 回发重载瞬间 contentDocument 短暂不可读 → 轮询直接放弃本关键字"的竞态：查询回发重载的间隙改为**继续等待**（而不是 break），带结果的页面渲染出来后照常点行——本科学校（SelUniversity）与专业选择器都受益；
- 兜底全量行扫描同样在回发间隙继续等待。

## v1.14.1（南理工本科专业选择器：按门类设置类别再查询）

- 🎓 **带"类别"下拉的选择器自动设门类**（巨能填同款思路）：南理工 SelBkdzZydm 专业选择器必须先在"类别"里选学科门类（哲学/工学…）关键字才查得出结果——现在按专业名自动猜测门类（测控技术与仪器→工学、计算机→工学、数学→理学…，11 类关键词表），猜测先试、其余门类依次兜底；设类别后等回发稳定再输入关键字查询；
- 🔑 关键字阶梯扩展为 全名 → 前 6 字 → 前 4 字（专业名匹配更稳）；
- 🧪 新增专业选择器类别过滤测试断言（自动设工学 + 点中行）。

## v1.14.0（吸收巨能填三表：长文同义词 / 通用标签别名 / 编码值字典）

- 📝 **长文同义词表**：获奖/科研/实践合成规则扩充 30+ 种标签叫法（如"参加科研工作、课外科技活动情况""参与科研工作发表学术成果等情况""奖惩记录""实习工作经历"等自动识别合成）；
- 🙋 **人工长文规则**：个人陈述/本人自述/自我介绍/申请理由/职业规划/研究计划/推荐人/备注等无档案数据的长文，从"未匹配"改为**明确标记跳过并提示"请人工撰写粘贴"**（不再误报漏填）；
- 🏷️ **通用表单标签别名**：吸收问卷式报名页常见叫法（"您的姓名""本科就读学校""本科专业全称""专业年级人数""意向导师""保研资格""调剂意愿"等 80+ 条）进字段匹配器；
- 🔢 **编码值字典**：纯码下拉选项按码值选中（婚否 1=未婚、政治面貌 03=团员、群众 13、非军人 0 等）；
- 🧪 新增 5 项测试断言。

## v1.13.7（修复悬浮面板 ✕ 关闭按钮点击无效）

- 🐛 面板标题栏的拖拽指针捕获（setPointerCapture）会把 ✕ 按钮的 pointerup/click 重定向到标题栏，导致关闭按钮点击无反应——现在点击 ✕ 不再进入拖拽逻辑，关闭恢复正常；
- 💡 收起面板后想再打开：点浏览器工具栏扩展图标 →「🪟 在当前页显示悬浮面板」。

## v1.13.6（再吸取三招：身份证推导地区 / 清理遗留弹窗 / 直写解锁）

- 🆔 **身份证前 6 位推导地区**（成熟填表软件同款）：档案里出生地/籍贯/户口地为空时，自动用身份证前 6 位区划码反查"省市区"填入（如 610102 → 陕西省西安市新城区），仅兜底、不覆盖已填值；
- 🧹 **清理遗留弹窗**：每次弹窗点选结束后自动点击 layui/lhg/art 等弹层「关闭」按钮并按 Escape，防止失败的旧弹窗叠在下一字段上挡住操作；
- 🔓 **三联直写前解除 disabled/readonly**：部分学校把显示框锁到弹窗点选为止，直写前先解锁再写入；
- 🧪 新增身份证推导、码反查、遗留弹窗清理 3 项测试断言。

## v1.13.5（地区三联直写：免弹窗，东华一次填对）

- 🎯 **地区三联直写**（借鉴成熟填表软件做法）：内置全国 **3224 条 6 位行政区划代码目录**（`src/core/region_codes.ts`，"省|市|区"→代码，与东华 treeSelectPage 同源数据）。出生地/籍贯/户口所在地等"代码框+名称框+显示框"三联，直接写 `610112` 这类区县叶子码 + 名称，**根本不开树弹窗**——秒填且不会再被"子级异步加载慢"卡住；
- 🌲 树弹窗降级为兜底：直写查不到码时才走弹窗，且弹窗内搜索关键字改为**6 位码优先**（树按码直查最准），其次市名；
- 🧪 新增 6 位码与三联直写共 5 项测试断言。

## v1.13.4（东华树弹窗提速：一次到位不再反复重试）

- ⏳ 点开父级后**子级等待从 6 秒加长到 12 秒/级**（实测东华"未央区"节点异步加载要 9 秒以上，等够一次成功，避免同字段重开弹窗三四轮）；
- 🚀 地区类值**跳过表格行关键字轮询**（对地区树毫无作用、纯浪费 15 秒+/轮），树点选失败直接转下一轮重试；
- 综合效果：单字段弹窗从 ~26 秒降到 ~10-15 秒，三个地区字段的弹窗流程大幅提速。

## v1.13.3（东华树节点点击目标修正 + 弹窗路径收敛）

- 🌲 树节点改点**最内层文本元素**（`<a><span>陕西省</span></a>` 的 span）：真实用户点击落点在最内层，此前点在外层 `<a>` 上不触发展开逻辑（"点开陕西省后子级不出现"的关键修正）；
- 🔎 关键字查询后（typed-kw + 点击搜索）**立即转交树点选**逐级点击，不再空等永远匹配不上的 tr 结果行——修复每个字段被表格行轮询吃掉 45 秒预算、树路径根本没机会运行的问题；
- ⚡ 同字段兄弟项（代码/名称/隐藏框共用同一弹窗）一轮失败后**不再重复弹窗**，三个字段的弹窗时间大幅缩短。

## v1.13.2（东华地区树"点开省后子级不出现"修复）

- 🌲 树节点点击前**中和空 href**（`<a href="">` 被点击会触发 iframe 自身刷新、把刚展开的树重置——这是"点开陕西省后西安市不出现"的直接原因之一）；节点点击改为**完整鼠标事件序列**（mousedown/mouseup/click），兼容绑定在不同事件上的展开逻辑；
- ⏳ 子级异步加载的等待从 2.4 秒延长到 **6 秒/级**；
- 🔎 层级找不到时自动用弹层**关键字框"搜索市名"**（东华正常操作流：搜→点→确定）定位后再补点；末级点选成功后自动点弹层「确定」；
- 🛡️ 关键字框只认 id/name/placeholder 带 keyword/关键字/txtWord 的输入框，绝不误写其他表单字段；
- 📊 保存后自动补填不再覆盖弹窗选择阶段的进度条显示。

## v1.13.1（东华大学地区树弹窗 + 弹窗阶段进度条保持）

- 🌲 地区树点选支持 **东华大学式分层树**（layui 弹层内嵌 iframe `treeSelectPage?lbcode=area`，节点为 `<a class="level0/1/2">` 链接）：节点按 level 层级优先匹配、省→市→区逐级点击展开、末级点选后自动点弹层「确定」提交；
- 📊 弹窗自动选择期间**进度条不再消失**：新增点选进行中标志，延时补填轮次与行任务在弹窗阶段不会误宣告"填充完成"，点选结束后才收尾；
- 🔝 进度条 z-index 提到最大值（2147483647），任何弹层遮罩都盖不住；
- 🏫 新增「东华大学」适配器（自动显示面板+提示），学校目录中标记"已适配"；
- 🧪 新增东华大学式分层树点选回归测试。

## v1.13.0（学校目录导航 + 行政区划驱动的地区弹窗）

- 🏫 **学校目录（报名入口）**：面板新增按钮，内置 82 所高校预推免报名系统入口网址，支持搜索、点击直达报名页，并可为每校标记"未开始/进行中/已填"（本地保存）；当前所在站点自动高亮，南理工/北邮标记"已适配"；
- 🗺️ **行政区划数据驱动地区弹窗**：内置全国省市区县数据（`src/core/regions.ts`，约 48KB）。籍贯/出生地/户口地等地区值现在按数据拆成"省→市→区"逐级点选（支持直辖市/自治区/不带后缀写法），查询窗口改用"西安市/未央区"等短关键字阶梯检索，替代整串地址（命中率大幅提升）；
- 🧪 新增地区拆分/关键字/学校目录共 10 项测试断言。

## v1.12.9（自动第二遍补填 + 完成提示语）

- 🔁 **兜底第二遍自动补填**：实测北邮页面第一遍点「添加」常因整页回发刷新丢上下文而漏行（如学习工作经历只落库一行），"再点一次一键填充"即可补全——现在 35 秒后自动等价重跑一次完整行任务，无需手动再点；
- 🛡️ **行任务互斥排队**：新的一键填充到达时若旧行任务仍在跑，结束后立即自动接力再跑一轮，任务不再因互斥静默丢失（修复"第一次点击毫无动静"的卡死路径）；
- 💬 完成横幅（100%）统一追加提示：**「若资料未完全填写，请再次点击一键填充」**；
- 🔍 `wfpProbe` 每条记录新增 `fired` 字段（std=标准回发 / void=location 求值 / click=原生点击），配合 `pbFired` 可一次判定"点击是否真的触发了回发"。

## v1.12.8（大号填充横幅 + 本页自动填写进度条）

- 📊 填充横幅升级为**大号顶部横幅 + 真进度条**：主标题、百分比、进度条、副说明四件套，22px 大字号，一目了然；
- 🔄 进度分阶段推进：扫描填充 → 常规字段（按已填/总数）→ 弹窗自动选择（i/n）→ 表格自动加行（按已添加/总条目）→ 延时补填 → 100% 绿色完成；
- 💾 **进度跨回发刷新不断档**：进度落盘 sessionStorage，自动加行引发整页刷新后横幅自动恢复并继续推进（此前横幅随页面刷新消失，这正是"提示看不见"的原因）；
- 🧹 完成态绿色停留 4.5 秒自动收起；「清除高亮/停止自动补填」同时收起横幅。

## v1.12.7（北邮"添加"不落库的正面修复：标准回发 + 自动换行）

- 🎯 `javascript:DoPostback` 链接点击改为**三轮换策略**：① 标准 ASP.NET 整页回发（设 `__EVENTTARGET/__EVENTARGUMENT` 后 `form.submit()`，与页面自带 `__doPostBack` 完全等价，北邮自定义 `WebForm_DoPostback` 包装最可靠）→ ② location 求值执行原 href → ③ 原生 click。一轮无效下一轮自动换方式；
- 🔁 北邮式逐行网格：**填完一行立即点本行「添加」落库**（自动换行/自动添加）；输入行里残留的"填了但没保存"内容也会被补点「添加」保存，不再卡在输入行里；
- 🛡️ 回发防护：页面整页刷新后旧文档立即停手（防误点新页面）、回发进行中不连点（防重复添加）；
- 🔍 诊断升级：`wfpProbe` 改为数组（每次点击的方式/事件目标/主世界函数源码片段，CSP 拦不住 location 求值探测），新增 `pbFired` 回发时间戳——报告可见，一次就能定位"点击是否真的触发了回发"。

## v1.12.6

- 🧪 回发执行改走 **location 求值**（`javascript:void(...)`，不受 CSP 拦截；注入 `<script>` 可能被页面 CSP 静默拦下）；
- 🔍 新增 `wfpProbe` 诊断：探测主世界 `WebForm_DoPostback` 函数是否存在及其形态（不生效时报告可见）。

## v1.12.5（北邮"添加"按钮与经历表定位修复）

- 🎯 经历表定位改为**优先"表头短单元格+多行"的真网格**（此前误匹配外层包装大表，行数恒为 1，任务空转）；
- 🧪 "添加/保存"类 `javascript:DoPostback` 链接改在**页面主世界执行**（注入 `<script>`），隔离世界 `.click()` 点不动北邮自定义回发函数——这是"点了添加却没加行"的直接原因。

## v1.12.4（北邮成员/经历漏填的最终根因）

- 🐛 行任务队列恢复"无进展即放弃本任务"分支（v1.12.2 重写时丢失）：页面没有对应类型表格时（如北邮无"学术成果"表），任务原地空转 15 轮卡死队列，导致**成员/经历任务从未执行**。现在会立即跳到下一个任务。

## v1.12.3

- 📋 `rowJobsDebug` 加入报告（此前只写存储未上报，看不见行任务实际干了什么）；
- 🔄 延时补填轮次会**唤醒因异常/上下文丢失而死掉的行任务**（互斥保证不并发重复点"添加"）。

## v1.12.2（修北邮"第二位家庭成员/第二条经历"漏填）

- 🐛 行任务加行成功判断改为**按任务自己的表格**量行数（此前用经历表判断家庭成员表，导致成员加行成功被误判为"页面刷新"而搁浅）；
- 🔁 点过「添加/保存」后不再空等，一律进入下一轮重试（原地加行→补下一条；整页刷新→载入恢复接管；未生效→再点）；
- 🛡️ 每个行任务独立容错（一个表格异常不再拖垮整条链）；
- 🧭 新增 `rowJobsDebug` 诊断（每轮类型/进度/行数变化），报告可见。

## v1.12.1

- ⏱️ 表格自动加行等待从 0.7s/1s 延长到 **2.5s**（成员/经历/奖项/成果四处统一）：北邮"添加"按钮回发较慢，此前新行未出现就放弃，导致第二位家庭成员与第二条经历漏填。

## v1.12.0（北邮 MasterTm 支持）

- 🏫 新增**北京邮电大学推免生招生系统**适配器（yzfs.bupt.edu.cn），自动显示面板；
- 🧩 经历表头识别扩展：北邮"学习起止时间/学习或工作单位名称/担任职务"式表头也能正确识别并自动填写；
- ℹ️ 说明：北邮 Apply1 的 CET4/6、年级总人数、排名、学期、家庭成员首行已能自动填写；"平均成绩"请在档案中补填；志愿院系/专业（选择弹窗）与课程成绩暂走「引导填写」人工处理。

## v1.11.2（根除南理工学校弹窗"循环"）

- 🎯 **真凶定位并移除**：通用弹层搜索框分支会把 Enter/「查询」误点到无关弹窗或主页面按钮，触发整页回发重载——南理工学校弹窗 2.7 秒循环重开正是它。已彻底删除该分支，关键字+查询统一走选择器窗口流程；
- 🧮 **调用次数硬上限**：每个字段的弹窗点选总共最多 4 次（记录在 sessionStorage，页面重载也生效）——无论什么原因造成的循环都会被强制终止并转人工提示；成功填上后自动清零。

## v1.11.1

- 🔒 **弹窗互斥**：同一字段已有点选流程在跑时，级联轮次不再重开弹窗（根除"卡在本科院校弹窗且循环弹出"）；90 秒卡死保护自动解锁；
- ⏱️ **45 秒硬超时**：弹窗点选整体限时，超时立即终止并落日志，绝不无限卡住；
- 🧭 每步落盘日志：`flow:open-done / opt / searchbox / selector-window / timeout`，任何卡点下次报告一步定位；
- ⚡ 关键字阶梯与等待精简（2 关键字 × 12 轮 × 0.6s），单字段点选总时长控制在 ~30 秒内。

## v1.11.0（借鉴成熟填表软件「巨能填」的南理工配方重写弹窗流程）

- 🔍 **不设类别下拉**：南理工学校/专业弹窗关键字直查即可（少一次回发，更快更稳）；
- 🪜 **关键字阶梯**：全名 → 前 4 字 → 前 2 字逐级查询；每个关键字独立"查询→等行→点行→校验"；全部失败后直接扫描弹窗已加载的全量行（服务器不返回查询结果也能点行）；
- ✅ **成对校验**：点行后轮询"码框纯数字 + 名框含中文"才算成功（不再把"点了行"当成功）；
- 🛟 **明文兜底**：弹窗点选失败时，从命中行文本提取编码，成对直接写入码框+名框（成熟软件同款最后一手）；
- 综合效果：南理工本科学校/专业码弹窗从此有 4 层保障（自动查询点选 → 全量行扫描 → 明文成对写入 → 横幅人工提示）。

## v1.10.1

- 🎯 学校弹窗根因修复：学校列表弹窗里"西安理工大学"的**文本格被当成可点选项**点击（点文本无效），导致流程提前返回、反复空转。现在点完选项后**验证值是否回填**，未回填立即继续走"输入→查询→选择行→对勾"的标准流程。

## v1.10.0

- 🧹 错位自愈修正：学校代码框命名是 `…wm`（专业码才是 `…dm`），且两框不在同一单元格——识别规则放宽为 `dm/bm/wm/cm` 结尾且不再要求同格，代号框里出现非数字文字即清空成对框重走弹窗；
- 🛡️ 弹窗点选全流程加异常捕获：任何一步出错都会写 `pick-exception` 日志而不是无声中断（此前学校弹窗"只打开无下文"即此类问题）。

## v1.9.9

- 🧹 **错位值自愈**：学校/专业"代码+名称"弹窗对里，代号框应为纯数字编码——一旦检测到代号框被填成文字（如学校名），自动清空该对两个框并重新走弹窗自动选择，纠正"代号填学校、名称填专业"的错位；
- 📜 学校弹窗点选流程补全日志（`selector-start` 起全程留痕），之前学校弹窗"只打开无下文"的问题可精确定位。

## v1.9.8

- 🔓 修复"弹窗不再弹出"：弹窗**已成功打开**（`opened`）不再计为失败，只有打不开才计数，失败上限提至 3 次；
- 🔔 **人工两步提示**：当弹窗已打开并自动填好类别与关键字、但服务器迟迟不返回结果时，弹窗保持打开，红色横幅明确提示"请点「查询」与绿色对勾"——你点两下，工具检测到回填后自动继续，且不再反复开关弹窗。

## v1.9.7

- ⏳ 选择器弹窗结果等待从 14 秒延长到 **24 秒**（南理工服务器"loading..."实测可超 15 秒），期间每 6 轮写一次进度日志；
- ✅ 行点选后等待 1.2 秒再检查关闭/补点，覆盖服务器"选中→回填→关窗"的慢节奏。

## v1.9.6

- 🔍 选择器弹窗修复"输完关键字不点查询"：去掉 Enter 键（避免未受控回发刷新 iframe 使后续点击落空），输完关键字后**重新定位再点「查询」**；
- 📜 弹窗各阶段**中途落盘日志**：卡住时报告能直接显示走到哪一步（类别→关键字→查询→等行）；
- ✅ 行点选后弹窗未关时**自动补点一次**行内选择按钮（部分 EasyUI 网格的对勾就是行内按钮本身）。

## v1.9.5

- 🛑 **弹窗不再反复弹**：每个字段的弹窗自动点选最多尝试 2 次（级联补填多轮不会每轮重开）；2 次仍失败转为「引导填写」人工处理，并停止打扰；
- ✅ **横幅不再卡住**：每轮弹窗点选结束立即收起"正在自动选择"提示并显示完成状态。

## v1.9.4

- 🎯 弹窗点选期间**强制显示红色横幅**（"正在自动选择：XX"），不再静默操作；
- ✅ 绿色对勾识别放宽：按"确定/确认/完成/选中"文字、check/ok 类名、图片 src（check/gou/dui 等）三路识别，先找弹窗内、再找父页面；图标型无文字确认按钮也能点中；
- ⏳ 选择器窗口等待延长至约 14 秒（20 轮×0.7s），覆盖南理工弹窗"loading..."长达 10 秒以上的慢加载。

## v1.9.3

- 🔴 填充横幅改为**红色高亮**：更大字号、红底白字、浅红描边+外发光、轻微呼吸缩放动画，进行中非常醒目；完成时变绿色。

## v1.9.2

- ✅ 选择器弹窗点选后自动补点"**确定/确认**"（绿色对勾）：选中表格行后若弹窗仍开着，自动寻找可见的确认按钮（文字"确定/确认/完成/选中"或 check/ok 类图标按钮）点击，完成关闭与回填，无需手动点对勾。

## v1.9.1

- 🏆 **奖励去重键修正为"名称+获奖时间"**：同名但获奖时间不同的奖项是两条不同记录，各占一行；只有名称与时间完全相同才合并去重（感谢指正）。

## v1.9.0

- 📢 新增**醒目填充横幅**：顶部大横幅实时显示"正在填充→已填 X 项→自动点选弹窗→自动加行→补填中→完成"全流程状态，不再干等；
- ⚡ 提速：浮层选项探测等待从 4 轮缩到 2 轮、选择器窗口轮询间隔 0.9s→0.5s（慢服务器仍保留 12 轮兜底）；
- 🏆 档案奖项**同名重复自动合并**并在结果中明示"有 N 条同名重复已合并"（此前第 1、2 条同名奖项被当成两条，实际只会填一行——这是正确行为，现在会明确告诉你）。

## v1.8.1

- 🐛 **根因修复（站点扫描立功）**：网格表格里存在"空白模板行"（只有图标按钮、没有输入框），此前被误当成可填空行选中，导致家庭成员/奖励/经历等全部"写不进去"（`wrote=false`）。现在**空行必须有关键输入框才算数**——成员/奖项/经历/成果四条填充链路同步修复；
- 🐛 奖励表格不再"假成功"：写不进空白模板行时不再计数与提示。

## v1.8.0

- 🔭 新增**站点架构扫描器**（"先读架构再操作"）：每次一键填充前自动扫描页面结构——网格表格（列头/数据行 HTML 样例/可写行数/加行按钮清单/有无保存按钮）、弹窗触发器、表单控件统计；报告同步输出 `siteScan`，从此不再盲猜 DOM，可直接据扫描结果生成精准适配。

## v1.7.2

- 🔎 家庭成员"空行"判断改为**只看姓名格可见输入框的值**（忽略单元格里零散的文本/图标干扰，此前可能把空行误判为"非空"而跳过）；
- 🩺 `familyDebug` 大幅增强：列索引、每行姓名格的 HTML 片段、每个成员的逐步结果（exists/no-empty-row/wrote=true/false），下次报告可精确到具体原因；
- 📢 已知交互提示：该页"添加一行"按钮点击后才会出现新行——v1.7.1 起工具会自动点它（含"保存"回发式加行）；若仍不自动加行，报告里的 steps 会说明按钮是否被找到。

## v1.7.1

- ⏱ **家庭成员"没填上"修复**：该系统网格是异步渲染（曾出现"loading..."长达 10 秒），此前填充时网格还没渲染出来。现在延时补填扩到 6.5s/12s/20s/30s 四轮 + 填表任务末尾再补一轮（10 秒后），网格一出现就补上；
- 🔎 家庭成员行名读取优先可见输入框（避免行内隐藏输入干扰"空行"判断）；
- 🩺 新增 `familyDebug` 诊断（成员清单/页面行名/可写行数/本表填充数），网格问题一眼定位。

## v1.7.0

- 🐛 **修复"家庭成员不填"的根因**："从本页提取档案"的家庭成员导入此前是**整表替换**——页面成员不全时会把档案里其他成员覆盖掉。改为**按姓名合并**（同名更新、新名追加、绝不删除）；
- 🩺 填充摘要新增 `profileLists`：报告里直接可见档案当前的家庭成员/奖项/经历清单，一眼定位"档案有但页面没填"还是"档案本身就缺"。

## v1.6.9

- 🐛 **修复"家庭成员乱套"**：家庭成员都"已存在"时，网格的空插入行此前会漏进通用字段匹配，被按属性名误填成**本人姓名/专业/电话**。现在家庭成员、奖励、经历、外语水平等所有网格表格的输入框一律标记为"表格区域"，永不参与通用匹配；
- 🧹 **自愈**：成员行"姓名"若是本人姓名（历史误填残留），填充时自动清空该行再填入正确成员；
- ✅ 专业码弹窗本轮已自动点选成功（等待加长生效），代码 080401 正确回填。

## v1.6.8

- 👨‍👩‍👧 家庭成员表格：**按姓名去重**（含服务器文本展示行）+ **自动加行**（无空行时自动点"新增一行/保存"，整页回发后断点续填），多成员不再丢失/重复；
- 🏆 奖励情况表格同样支持**自动加行与断点续填**；
- ✂️ 奖励名称智能拆分：档案名若为"西安理工大学·尚真笃学先进个人"（反向提取把单位并进名称），自动拆成"单位=西安理工大学、名称=尚真笃学先进个人"，不再整串写进"奖励名称"；
- ⏳ 选择器弹窗行匹配重试增至 12 轮 ×0.9s（应对"loading..."慢加载）。

## v1.6.7

- 🔍 选择器窗口流程重写为**三阶段、每步重定位**：类别选中后（AutoPostBack 可能重载 iframe）等待并重新定位 → 关键字+查询（重定位后操作）→ 行匹配每轮重新定位（应对回发慢加载），修复"查询后列表始终无匹配行"；
- 👁 iframe 评分加入**可见性加权**（当前打开的窗口优先于历史隐藏窗口），避免把关键字打进已关闭的旧弹窗；
- 🔤 行匹配改为**宽松三级匹配**（完整值 → 前 4 字 → 前 2 字，取最短行），兼容列表里"测控技术与仪器（080301）"等带后缀写法；无匹配时记录列表样例文本供诊断。

## v1.6.6

- 🎓 选择器窗口支持**类别下拉自动推导**：本科专业码等弹窗带"类别"过滤（默认"--请选择--"导致搜不到），现在按目标值自动推导学科门类（测控技术与仪器→工学、计算机→工学、数学→理学…）并选中后再搜索；
- 🛰 `pickDebug` 增加**分步日志**（类别/关键字/查询/行匹配/控件点击每步都有记录），下次报告可精确到"卡在哪个动作"；
- 🔗 代码+名称成对字段按"字段+单元格/行"去重，跨单元格的成对字段也只开一次弹窗。

## v1.6.5

- 🐛 **修复"弹窗压根没打开"**：判断输入框是否"已选过"时误用了 `fieldValueOf()`（对输入框返回 `值|checked`，空框也返回 `"|false"` 而非空串），导致所有输入型弹窗字段被当成"已选过"而整体跳过、`pickInPage` 从未执行。改用专门的 `controlEmpty()` 判断；
- 🖱 弹层打开改为**多策略逐级尝试**：外层 linkbutton `<a>` → 触发元素 → 输入框本身 → 完整鼠标事件序列，任一策略弹出了可见窗口即停止；
- 🛰 新增 `pickDebug` 调试记录（触发策略/是否弹出/最终结果），随字段报告输出，可远程定位"卡在哪一步"。

## v1.6.4

- 🖱 回发型选择器窗口点选修复：南理工选择器表格每行的「选择」是 `<input type="image">` 图标按钮（无文字），此前识别不到导致最后一步点空。现在行内控件按"文字→类名→图标 image 输入→末位控件"逐级兜底，并加大回发等待与重试（6 轮）；
- 🔁 弹窗点选后的联动补填增加第二轮（6.5s/12s），确保被页面脚本清空的院系/专业等字段稳定恢复。

## v1.6.3

- 🪟 新增**回发型选择器窗口通用自动点选**（南理工本科学校/本科专业码等）：弹窗是 iframe 且含"关键字+查询+编码/名称表格+选择"时，自动输入关键字→点查询→在表格行中定位目标→点「选择」完成回填；按 URL/内容评分挑选正确 iframe，排除日期选择器，不影响其他类型弹层；
- 🔁 弹窗点选后 6.5 秒补填一次：防止选择本科学校后页面脚本清空联动字段（院系/专业等）；
- 🧭 兜底顺序优化：地区类值先走省市县树、院校/专业类值先走选择器窗口，两者互为备选。

## v1.6.2

- 🏆 新增**奖励情况表自动填充**（南理工"学业、其他"页：时间/奖励单位/奖励原因/奖励名称）——时间与名称来自档案奖项，单位默认填本科学校、原因默认填奖项级别（已做提示，请人工核对这两列）；
- 🔍 弹窗点选增强：识别 EasyUI datagrid 行选项、窗口容器（`[class*="window"]`）、搜索框输入后自动回车并点击"搜索/查询"按钮、选项加载重试 4 次（应对慢速 iframe 弹层）。

## v1.6.1

- 🌳 地区弹层全自动点选增强：支持**弹层嵌在同源 iframe 内**（EasyUI"视窗"常见形态）、节点文字宽松匹配（"陕西"与"陕西省"互通）、逐级展开重试 5 次（应对异步加载）；
- 🖱 代码+名称成对字段只开一次弹窗（同一单元格只处理一个触发按钮）；
- 🔍 浮层选项/弹层搜索框搜索范围扩展到同源 iframe。

## v1.6.0

- 🐛 **根治"将截断字符串或二进制数据"**：找到真凶——南理工"籍贯所在地/出生地/户口地"是**代码+名称成对字段**（如 `txtJgdm` 地区代码 + `txtJg` 名称），此前工具把完整地名直接注入了**代码列**（数据库只存 6 位代码，必然截断）。现在带"选择"按钮的输入框一律走弹窗点选，绝不注入文本；
- 🌳 新增**省市县树形弹层自动点选**：弹窗打开后按"省→市→区"逐级点击树节点，自动把代码和名称同时选好（籍贯/出生地/户口地全自动）；
- 🧹 日历等工具 iframe 不再执行填充、不再覆盖主页面诊断数据（此前会把字段快照/摘要冲掉）；
- 🩹 快照不再记录"更新照片/清空/今天"等按钮值。

## v1.5.9

- 🔁 **保存后自动恢复被清空的字段**：同一标签页内只要点过「一键填充」，之后每次点保存导致页面刷新，工具会自动分 3 轮补填被清空的字段（兼容 EasyUI 异步就绪），并自动点选弹窗字段（籍贯/出生地/户口地）——不再需要每次保存后重新手动填；
- 🩺 报告新增 `fillSummary`：上次填充每个字段的**逐字段状态**（filled/profileEmpty/picker/failed + 值预览），保存后信息"消失"时发报告即可直接定位原因；
- 🔧 弹窗自动点选跳过已回显的字段（保存成功后不再重复弹窗）。

## v1.5.8

- 🐛 **识别服务器渲染的"文本展示行"**：南理工经历/成果网格中已保存的行是纯文本（无输入框），此前会被当作"不存在"而重复填入插入行。现在展示行也算"已存在"——只填新条目、绝不重复；插入行正常接收下一条经历。
- 🩹 长字段保守截断（通讯地址≤50、邮箱≤40、姓名/拼音≤30 等），防止学校数据库列长过小触发"将截断字符串或二进制数据"；
- 📸 **保存前值快照**：每次填充后把已填值写入 sessionStorage，即使保存失败清空页面，报告里也能还原"保存前"的值，便于定位真正超长的字段；
- 🔁 自动加行流程区分"原地加行"与"整页刷新"，原地加行后立即继续填下一条，不再留下悬空任务。

## v1.5.7

- 🐛 修复"截断"依旧：此前去重保护会把页面上已存在的经历行整个跳过，旧长格式时间（`2020年9月 至 2023年6月`）原样留在格子里，保存仍然报错。现在对已存在的条目**只归一化时间格**为 `YYYY.MM-YYYY.MM`（内容不动、不重复加行）。

## v1.5.6

- 🐛 修复保存报错"将截断字符串或二进制数据"：学习/工作经历起止时间改为紧凑格式 `YYYY.MM-YYYY.MM`（限长 20 字符），避免超出数据库列长；
- 🔁 回发恢复时自动补回被失败保存清空的常规字段（重新执行一键填充）。

## v1.5.5

- 💾 南理工"保存后多一行"流程全自动：行数不够且无加行按钮时，自动点击页面「保存」按钮（回发刷新后由断点续填接管），逐条填写直到最后一行；**最后一行的保存仍由你手动点击**（不越界自动提交）。
- 🖱 同步支持"点击空白模板行激活编辑"（EasyUI click-to-edit 网格）。

## v1.5.3

- 🐛 **修复"有行无字"真凶**：经历/成果表格的"没有数据/暂无数据"占位行（无输入框的提示行）被误当作空行写入，导致真实输入框行永远轮不到。现在候选行必须包含输入框、占位行自动跳过（同步与自动加行两条路径均已修复）。

## v1.5.2

- 🔍 字段报告新增值快照与经历/成果表数据行快照（调试网格填充用，截断显示）；
- 🔧 单元格写入兜底：找不到 text 输入框时自动尝试 textarea/contenteditable。

## v1.5.1

- ⏱ 修复回发后新增行**有行无字**：恢复填充前等待 EasyUI 网格异步渲染稳定（轮询行数），写完后 2.5s/5s 再补写两次（纯填充、不点按钮、幂等），防止内容被网格重渲染抹掉。

## v1.5.0

- 🖱 支持 **EasyUI 无文字图标型"新增一行"按钮**（表格末列 `l-btn-empty icon-search` 图标链接），南理工自动加行打通；
- 🛡 **重复填写保护**：学习/工作经历与学术成果表格在填充前先识别页面上已存在的条目（按单位/标题匹配），已存在的条目**不再重复填写、不再重复加行**——手动加的行和已填的内容都不会被覆盖或复制。

## v1.4.1

- 🔁 **自动加行断点续填**：ASP.NET 老式系统点"新增一行"会整页刷新导致任务中断，现在用 sessionStorage 记录进度，页面刷新后自动恢复继续填写（南理工场景）；
- 🖱 "新增一行"按钮识别扩展：支持 `<input type="submit">` 型按钮（读取 value 文案）、camelCase 类名（btnAdd/addRow 等）、跨表格误命中防护。

## v1.4.0

- 📋 学习 / 工作经历表格支持**自动新增行**：档案里有多条经历时自动点击"新增一行"逐条填写（与学术成果一致）；
- ✍️ "学习和工作经历"长文本域改为从档案「学习 / 工作经历」列表汇总合成（`起至止，单位，职务`）；
- 🛠 修复"新增一行"按钮跨表格误命中（先在表格自身内部查找按钮）。

## v1.3.0

- 📝 档案「社会实践 / 学生工作」字段调整为：**时间 / 活动名称 / 担任职务 / 具体内容**（旧数据自动迁移：单位/组织→活动名称、描述→具体内容）；长文本合成与反向提取同步适配。

## v1.2.0

- 🎓 南京理工大学预推免系统适配（ASP.NET WebForms）；
- 📋 新增档案列表「学习 / 工作经历」（起始/结束时间、单位、职务），支持"学习或工作经历"表格自动填写与反向提取；
- 🖱 弹窗选择框识别升级：支持"显示框+隐藏编码"成对结构（如 `txtCsd`+`txtCsdm`），出生地/籍贯/户口地类弹窗不再注入文本而是走弹窗点选；
- 新增「户口所在地」字段与规则；「现役军人」（不带"码"）关键词；
- 🛡 误匹配修复：标签识别不再把相邻的表格/标题当标签；隐藏输入框仅限"弹窗选择类"字段参与匹配（修复 hdIndex 被误判为学号）；触发按钮查找限定在同一单元格，修复全表单误判为弹窗框的问题。

## v1.1.0

- 🧭 **站点向导**：自动识别页签/「下一步」按钮，逐页填充走完全部页签（最多 15 页，排除提交/缴费类按钮，遇无变化自动停止）；
- 🩺 **提交前体检**：扫描必填缺失、格式错误（身份证 18 位+校验位/手机/邮箱/邮编）、一致性风险（出生日期 vs 身份证、排名 vs 总人数），风险清单点击直达字段；
- 🔄 **规则热更新（默认关闭）**：档案编辑器底部可开启——仅从配置地址下载规则文件（平台适配器+字段规则），不携带任何个人数据；保持关闭即完全离线；
- 修正测试用身份证校验位错误；新增体检/向导/远程规则合并共 12 项断言。

## v1.0.0（发布版）

### 核心功能
- 本地档案（基本信息/教育背景/四六级/获奖/科研/实践/家庭主要成员/自我陈述多版本/报考意向），JSON 导入导出、随机测试数据一键生成；
- 一键填充：通用字段匹配引擎（中文标签/拼音缩写属性/编码选项 `010101|哲学` 按名称匹配）、React/Vue 受控组件安全写入、日期格式自适应（`2003-05-12`→`20030512`/`2003/05/12`/`2003年05月12日`）、出生年月/院校省份/四六级通过与否等档案推导；
- 引导式填写：弹窗选择框（只读/禁用/隐藏/Show 显示框 + 选择按钮）自动打开选择器、浮层选项自动点选（含弹层搜索框过滤）、联动下拉自动重试 3 次；
- 表格填充：家庭成员表（跨表格连续计数）、外语水平表（名称/成绩/取得时间）、学术成果表（自动"新增一行"逐条填写）；
- 反向提取：从已填页面一键导入档案（常规字段/家庭/外语水平/学术成果/奖励/学习工作经历，跨表上下文识别）；
- 跨 iframe 填充与字段报告聚合；漏填项复制（含自我陈述字数速查）；档案编辑器缺失项横幅提醒。

### 平台适配
- 大连理工大学研究生报考服务系统
- 西安电子科技大学（gsapp 研招统一平台 + authserver 统一认证）
- 华东理工大学招生考务管理平台
- 河海大学研究生招生管理系统
- 南京航空航天大学研究生报考服务系统
- 通用研招/推免域名自动识别（yjszs/yzss/yzsbm/gsapp/tmybm 等）

### 质量
- TypeScript 全量类型检查；123+ 项自动化断言（jsdom 模拟 10 类真实表单结构）；Edge headless 真实浏览器端到端回归；
- 隐私：全源码零网络调用、零统计代码，数据仅存本机（见 PRIVACY.md）。
