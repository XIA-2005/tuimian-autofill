# 隐私说明（预推免填表助手）

生效版本：v1.0.0

## 数据收集

**本扩展不收集、不上传、不共享任何个人数据。**

- 你填写的个人档案（姓名、联系方式、成绩、经历等）仅保存在**本机浏览器**的本地存储（`chrome.storage.local`）中；
- 默认状态下扩展**完全离线**：不包含任何网络请求代码（无 `fetch`/`XMLHttpRequest`/埋点/统计 SDK），数据永远不会离开你的设备；
- **可选的"规则更新"**（默认关闭）：在档案编辑器底部手动开启后，扩展仅会从你配置的地址下载**规则文件**（平台适配器与字段规则），用于覆盖更多学校系统；该请求**不携带、不上传任何个人数据**，且只允许访问规则文件域名（`raw.githubusercontent.com`）；
- 「复制字段报告」功能只复制**页面字段结构**（字段标签、控件类型），不包含任何填写内容；
- 「复制漏填项」复制的内容仅进入你的**系统剪贴板**，由你自行决定粘贴到何处。

## 权限说明

| 权限 | 用途 | 风险 |
|---|---|---|
| `storage` | 在本机保存你的个人档案与设置 | 仅本机可见 |
| `clipboardWrite` | 「复制漏填项 / 复制字段报告」写入剪贴板 | 仅在你点击按钮时触发 |
| `host_permissions`（仅 `raw.githubusercontent.com`） | 可选的规则更新功能下载规则文件（默认关闭） | 仅在开启规则更新并点击更新时访问该域名 |
| 页面访问（content script，`http/https` 页面） | 在报名页面上执行自动填写 | 不读取页面上的其他数据、不拦截网络请求 |

## 使用边界（请务必遵守）

- 工具**不自动提交**任何表单，提交前请人工核对；
- **验证码、短信验证、登录**全部由用户人工完成，工具不会尝试绕过；
- 请勿将本工具用于任何违反学校规定或法律法规的用途（如批量注册、抢占名额）。

## 卸载与数据删除

在 `edge://extensions` / `chrome://extensions` 中移除本扩展，本地档案即被一并删除。也可在档案编辑器中使用「导出 JSON 备份 / 清空重置」。

## 联系方式

如对本隐私说明有疑问，请通过扩展分发渠道（商店页面）与我们联系。
