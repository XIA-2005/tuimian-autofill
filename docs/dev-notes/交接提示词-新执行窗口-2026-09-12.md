# 启动提示词（执行者换班 · 2026-09-12）· 接替 QWEN 执行窗口

> 你是 v10 轮的**执行者（单写者）**。审查者=DeepSeek（对抗审查+oracle 签名方 P6），第三方审计=GPT（只读、产出仅落 `docs/analysis/v10-review-*/`），用户转达。
> **铁律：先跑 §0 核对，再信本文件任何转述（包括它自己）。** 与实测不符处以实测为准并报告差异。

---

## 0. 接手核对（期望值已给全）

```bash
git log --oneline -4        # 期望 HEAD=“v10: executor handover prompt”提交，其下依次 0ad9a39(rev3)→11629be(rev2)→9efd196(v10.4)；本地与 origin 平齐
git status --short           # 期望：5 个既存 M（ds-v9-verify×3、implementation-progress-v9、regression-coverage-v9）+ ?? .deploy-xiaaaaa-v203/ + ?? nul（这些永不入库、不动）
sha256sum "docs/analysis/任务书-v10-正确率与范围收敛-2026-09-11.md"   # 期望 df0539e884384de6db4a3a2f5bc10850eeac039ef8787bb13233d9da6a9dce65
node tools/v10-hashes.cjs check          # 期望：受控一致 140 | 漂移 1 | 缺失 0 | 未声明 0 | 存量漂移 0 | 外部 3，exit 1；唯一 DRIFT=touch-lists.json（已披露待签漂移，设计内）
node docs/analysis/v10-review-2026-09-11/probe-oracle-validate.cjs docs/analysis/v10-review-2026-09-11/oracle-draft-F01.json
#   期望：仅 1 项 FAIL=A6(3<15)（D1 试签批既定形态），exit 1
gh run list --workflow=offline-gates -L 3   # rev3(0ad9a39) 的 run 应 success；若仍 queued 等待并记账
```

必读（按序）：任务书 v10.4 → 本文件 → 账本 `docs/dev-notes/v10-execution-ledger.md`（尾部 L-F01b/L-R6/L-R5）→ `docs/analysis/v10-review-2026-09-11/复审-F01三校试签批-2026-09-11.md`（含 rev2/E4 裁定；注意其"待 rev3 复跑"状态）→ `oracle-复核判据-F01-2026-09-11.md`（+判据 v1.1 E1-E3）→ 记忆 `tuimian-v10-execution-state`（本会话自动加载，含全轮史）。

## 1. 不可违反的纪律（全部已写入任务书，这里只列操作面）

1. **RD 红线**：不改/不复制 `D:\巨能填系统-1.8.9`（且**禁在其目录内执行任何包脚本**，只许文件系统只读）；不自动保存/下一步/申报提交（唯一豁免 RD-C2 登录表单四条件）；不自动上传附件；不存凭据；不引入 LLM；页面原文与资料值不落盘（P9）；禁止"vs 竞品 X%"假数字（P8）；已声明未实现禁静默降级（RD-8）。
2. **签名纪律 RD-7 + §2-2**：任何 `update <卡>` 只在审查者对该内容出具过审后执行；**update 前必须跑 `node tools/v10-hashes.cjs check` 并把输出全文贴进账本条目**。新文件先登记 touch-list 再开工（顺序错了会产生"已签文件 DRIFT"待处置——F01 已踩过一次）。
3. **交付 7 条缺一记 failed**：文件:行号证据｜命令+退出码（跳过/超时≠通过）｜bench 四元组｜F02 对应红翻绿｜负向自检=具名断言+非零退出｜`npm run test:offline` 全绿+`git diff --check` 干净｜账本条目+F04 重签+审查者独立复跑。
4. **禁止**：改断言迁就失败；把当前行为固化成期望（oracle 与 F02 写目标行为）；事后补记流程（范围变更改任务书卡文）；手抄数据（凡能从 DOM/合同反推的一律脚本推导）。
5. 提交只 stage 本卡声明文件；`--strict` 已知恒红（残留 2，待修，见 T1）；`gh run watch` 偶发误报 exit1 → 用 `gh run view <id> --json conclusion` 复核；git push 偶发 Connection reset → 重试。

## 2. 当前精确位点（2026-09-12）

- **已签名托管**（审查者探针回算 PASS 10/FAIL 0 @2f8bdad 独立验证）：F03 六条（含 offline-gates.yml 签入、tmp runner `ABSENT→DELETED` 墓碑）+ F04 四件（工具/touch-lists/**baseline+tree 两锚**）。
- **F01**：rev3（`0ad9a39`）已推——修完审查者全部拒签项（D2 一条 + F-3 + E4 `pwd→explicit-refusal`），`expect/deferredTo` F-2 二分与 E1/E2 机械推导已被其裁定采纳冻结。**卡在：等审查者对 rev3 复跑三项（定位子回查/一致性核对/校验器）→ 出 `oracle-F01.signed.json`。**
- 度量冻结（P1–P9）：四分类互斥优先 `越界填>填错>漏填`+拒填正交带理由；越界按**台账口径**（`registerWriteOwnership` 每次调用+每次 DOM 赋值，attempted/committed 区分，**禁尾态**）；判等**独立**（禁 import `isSemanticEqual`/`readNativeControlValue`）；优先级 `越界填=0＞填错=0＞拒填可解释＞漏填最小化`。
- 卡序（任务书 §3）：`F01→F02→B4→A1→A2a→A2b→A3→A4→A5→A6→A7→A8→B1→B2→B3→B5→B6→C1→C2→D1→D2→D3`；B1–B3 晚于 A5/A6；A4 依赖 A1/A2b；D 组等用户授权+清单。
- **向用户要**（不阻塞 T1/T2）：①≥15 校扩批与 D 阶段共用的**真实目标校清单+入口 URL**；②D1 CDP 授权。合成夹具只覆盖形态族，覆盖不了真实脏形状。

## 3. 任务队列

### T1（等签名，先到先做）：签批落地批
签名到后依序：①`--strict` 修（排除 `externalMod`，保留 declared/declaredMod 加严）→ `update F04 tools/v10-hashes.cjs docs/analysis/v10-hashes/touch-lists.json`；②`update F01`（草案/抽样/生成器/bench 产物）清 touch-list 漂移；③`update F00`（任务书+竞品差距文档）；④check **exit 0** + 每步前贴全文（§2-2）→ 账本 L-F01c。

### T2（现在即可并行，禁跑绿）：bench 骨架
`test/bench/`：`run.mjs` 走 `src/core/fill-pipeline.ts` 真实入口 + 四分类；P4 **只读台账包装**（不改生产登记）；P5 独立最小读取+字面判等；P9 脱敏报表；确定性（固定 seed/时间基准，同输入同四元组）；`--negative-overfill`（注入一次期望外写入→具名 exit 1）；`npm run bench` 并入 `test:offline`。**未冻结期望表不得跑绿**——无 signed oracle 时 bench 以 `--self-test` 模式运行（合成微型夹具+内联期望，专证负向路径），并在 touch-list F01 内登记。

### T3：≥15 校扩批（前置=用户清单）
全走 `test/gen-oracle-draft.cjs` 单源扩展（禁手编 JSON）：每校 DOM 反推全集三分层；**E3/C3 必需**：items 须含"档案可能为空"字段（否则漏填不可测）；每 refusal `expect` 二分且 **silent-no-write 必须带 deferredTo**（审查者冻结规则）；折叠映射入文档；过 `probe-oracle-validate.cjs` 后交审查者扩签批。

### T4：F02 红相位
R1–R7/S1–S4 **目标行为**红断言入 `docs/dev-notes/correctness-gaps-2026-09-11.md`（禁固化现状）；F01 门禁"清单内已知红按登记放行"；每条标翻绿卡号。

### T5+：按卡序推进 B4→A1…（A1 开工前重读任务书 INV-A1 四不变式与各负向断言；F05 雷区表 `docs/dev-notes/v10-影响面-F05.md`；A5/A6 标尺=`test/run.ts:254/282/315` 紧急槽必须保持绿；A7 必复跑蓝三联 fixture `'61|10698|西安交通大学'`；B3 夹具自建 contenteditable 裸 div+编辑器实例各一，并同步 matcher/checker 名单与计数断言）。

## 4. 治理工具心智模型（勿重新发现）

`tools/v10-hashes.cjs` 四模式 baseline/tree/check/update：**锚=git blob@9a0f40a + EOL 归一**（抗重建洗白、抗 autocrlf 假漂移）；check 分类=DRIFT/MISSING/DECLARED-NEW/UNSIGNED-NEW/DECLARED-MOD/TREE-DRIFT/EXTERNAL-MOD；**声明即允许、未声明即拒**；tree 只在冻结时建一次（重建=再锚定风险，须记账本）；externalDrift 三文件豁免表见任务书 §5.1（永不入卡账、也不许"顺手修复"）；`card-signatures.json` 无法自锚（各卡 update 自然追加）。
e2e 开关：`PW_HEADLESS=1`、`PW_BUNDLED=1`（跳 Edge 复现 CI 内置路径）、`PW_EXTDIR=空目录`（具名负向）；e2e 四脚本统一 `channel:'chromium'` 回退惯例，serviceworker 30s 具名失败。

## 5. 交审查者格式（每批一条消息）
卡 ID｜改动文件（**含新增**）｜逐条命令+退出码｜负向自检失败形态原文｜bench 四元组（若有）｜账本路径｜**请其复跑命令清单+期望值**。审查者不复用你的测试、自建探针回算——所以一切声明以可重跑为唯一凭据。

—— 交接完。跑 §0，若有任一不符：停，把差异报告给用户转审查者，不自行"修复现场"。
