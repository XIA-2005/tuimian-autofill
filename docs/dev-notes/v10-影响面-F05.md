# F05 · 影响面扫描清单（只读，2026-09-11）

> 目的：B1/B3（检测面扩张）、A7（码表扩容）、A5/A6（角色否决）、B4（driver 收口）会推翻下列断言的隐含前提。逐条列 位置｜依赖前提｜受影响卡｜建议改法。冻结于任务书 v10.1 §3-F05。

## 1. 精确计数断言（B1/B3 检测面扩张的雷区）

| 位置 | 依赖前提 | 受影响卡 | 建议改法 |
|---|---|---|---|
| `test/run.ts:294` `pickerItems.length === 10` | picker 状态条目恰好 10（fixture 无 checkbox/contenteditable 进入 picker 桶） | B1,B3 | 按状态桶计数不受新控件类型影响则保留；若 `detectAllFields` 扩面改变分组，改为"含期望 10 项且无额外 picker"集合语义 |
| `test/run.ts:296` `stats.picker === 10` | 同上（统计口径） | B1,B3 | 同上；total/filled 类联动统计一并核 |
| `test/run.ts:492` `progress.current===3 && total===4` | fixture 参与收口控件数固定 | B1,B3 | B1/B3 交付时若 fixture 扩控件，须显式更新并登记 |
| `test/regression/run.ts:375` `els.length === 2`（同名元素留痕自校验） | 匹配器对同名元素恰好命中 2 | B1,B3 | 检测面变更须复跑该 HARD；必要时收窄选择器 |
| `test/check-adapters.ts:66` `declared==contract 完整集合` | 包字段合同与证据样本逐一对应 | A7,B2,B4 | 任何 driver/字段改动同步证据 JSON；B4 移除声明时同步 |
| `test/check-adapters.ts:132` `SCHOOL_ADAPTER_PACKAGES.length > 0` | 下界非精确 | — | 安全，无需改 |

## 2. 角色词既有行为断言（A5/A6 边界标尺——这些必须保持绿）

| 位置 | 现有正确行为 | A5/A6 约束 |
|---|---|---|
| `test/run.ts:254-255` | `jxlxr/jxlxrdh`（紧急联系人）从档案紧急联系人字段取值，**不误填本人手机** | A6 角色识别的正例基线：紧急槽≠本人槽 |
| `test/run.ts:282-283,315` | "家长姓名/联系人→紧急联系人槽"、"亲属电话→紧急联系电话" | **A5 禁回落是"本人手机槽禁固话/他值"，不是"紧急槽禁电话值"**——卡文措辞须显式引用这三条为标尺 |
| `test/run.ts:634` | 家庭表 disabled 值原样保留（写保护） | A2b 写前解锁（unlock.ts）与之一致性复核 |

## 3. 地区/码表（A7）

- `region_codes.ts` **已含** 71 台湾省 / 81 香港 / 82 澳门 条目（实测 grep），缺的只有 `REGION_TREE`（31 省）→ A7 只需补树到同口径；`test/run.ts:2634/2638` 拆分断言不涉及台港澳，无既有断言会被打破。
- 蓝色三联弹窗夹具 `test/run.ts:2862-2882` 硬编码省码 `'61'` + 选项 `"61|10698|西安交通大学"`：**A7 把 picker 搜索词从名称换为码后必复跑**；若 fixture 的查询响应只按名称过滤，需把 fixture 查询逻辑改为"名称或码皆可命中"再验。
- `test/run.ts:583`（南理工双分支）等 programs 断言与 A7 无关；B6 新增双分支校时按同款写。

## 4. 其他发现

- 全 `test/` 无 `contenteditable` 夹具（实测）→ B3 第一步须自带 fixture（含 UEditor/quill 实例与裸 div 各一），并预期 §1 表内计数桶变化。
- 三个已带 `channel:'chromium'` 回退的 e2e（dependency/manual-handoff/deadline）在 CI 无计数断言风险（其断言为行为型），F03 修复已把 playwright.mjs/run-e2e.ts 收敛到同一惯例。
- `.github/workflows/offline-gates.yml:3` 注释"本工作流尚未在真实 CI 运行"**已过时**（gh 实测已跑 3 次）；注释更新留给 D3 收口批次，不在 F03 强改。
