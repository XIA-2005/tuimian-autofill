# 给 DeepSeek V4 Flash 的执行启动提示词

把下面整段发送给实施 AI。让它读取本地文件；如果它不能访问你的电脑，则上传完整 PLAN 和审查意见，不能只发送本提示词就声称已读。

---

你是本项目的实施工程师。请按任务卡逐步实现，不要重新生成一套替代方案。

项目目录：`D:\deepseek-work\tuimian-autofill`。

先完整读取：

1. `D:\deepseek-work\tuimian-autofill\docs\analysis\DeepSeek-V4-Flash-实施PLAN-v3.md`
2. `D:\deepseek-work\tuimian-autofill\docs\analysis\任务书审查意见-2026-09-09.md`
3. 项目实际适用的 AGENTS.md，以及已存在的 `docs/dev-notes/implementation-progress-v3.md`。

执行要求：

- 从 P00 开始；若账本已存在，先核实上一个 passed 卡的文件与测试证据，再进入下一卡。历史任务书中的“都已源码核实”“已验收”不能代替你当前检查。
- 一次工作单元只处理当前任务卡。验收通过且上下文足够时，可记录账本后继续下一卡，不必每张卡再次问我是否继续；前置条件未满足时不得推进依赖任务。
- 第一轮 P00 只记录基线，不修改业务源码。之后按文件范围和顺序实施，不在第一轮就重写 fillAll、改整个异步调用链或新建全套架构。
- 保持完整一键填充、已支持的弹窗精确点选、动态表加行、人工接管和下一轮重试。不恢复安全辅助填写/兼容体检，不用全局禁写掩盖正确率问题。
- 普通字段冲突只停止该字段，依赖失败只停止子树；未适配、缺样本、无 fingerprint 不等于整页禁止填写。
- 不新增自动保存、下一步、最终提交、账号自动化、上传、外部 AI 请求；保留 OCR 当前默认与用户配置。不要混淆 picker 选项确认与报名最终提交。
- 不执行真实高校认证网络操作，不运行 test:e2e:real。需要实页时在账本列出明确缺口，不拿我的 Cookie/账号自行测试。
- 原值/目标值/任务快照仅按 PLAN 放在内存；不写页面 sessionStorage、不放 DOM data 属性、不进入默认复制报告/日志。现有数据格式需要兼容，不能批量清空用户数据。
- 不修改竞品目录，不删未跟踪文件，不升级依赖，不自动 git reset/clean，不提交或发布。
- 新增代码按项目 TS/MV3 实际环境实现，函数/变量驼峰，常量大写下划线，新增/修改函数前有中文功能说明，关键算法有中文规则注释。不要把 STM32/HAL 工具链引入本项目。
- 测试必须验证生产函数和真实打包扩展；不能复制一套测试专用实现。新测试必须挂进实际 runner，既验证不误填，也验证应该填写的内容确实填写。
- 每张卡记录：变更范围、测试命令与退出码、Rxx 场景结果、覆盖变化、未验证限制和下一卡。不能把未运行、跳过或超时记为通过，不能改断言来迁就失败。
- 遇到环境/既有失败，记录并隔离最小阻塞；不要无限重试或顺手大修。缺实页证据只阻止相关 live 验收/生产策略启用，仍可完成不依赖它的已授权离线任务。

请现在执行 P00：读取源码和检查基线，建立进度账本。给出实际证据后，再按 PLAN 进入 P01。不要只复述计划，也不要声称已实现尚未做的任务。

---

## 换对话后的续接短提示词

继续 `D:\deepseek-work\tuimian-autofill` 的 PLAN v3 实施。先读取 `docs/analysis/DeepSeek-V4-Flash-实施PLAN-v3.md` 和 `docs/dev-notes/implementation-progress-v3.md`，核查当前 Git 差异及上张卡证据，然后执行第一张前置条件已满足且尚未通过的任务卡。保留全部用户改动，执行完更新账本。不能仅凭上一条 AI 消息中的“完成”跳过验证。
