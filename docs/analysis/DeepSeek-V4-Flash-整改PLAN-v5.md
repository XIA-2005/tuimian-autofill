# DeepSeek V4 Flash 整改 PLAN v5

项目：`D:\deepseek-work\tuimian-autofill`。日期：2026-09-09。
依据：`DS-v4完成情况复审-2026-09-09.md`、v4计划及本次实际源码。v3/v4未取消的产品约束与验收场景继续有效。

## 0. 本次要解决什么

不要再把“独立函数通过”当成“生产全链通过”。此次主要问题是合同、通用、异步、汇总之间没有共享同一目标及结果语义；给其中一条路径加保护，另一条仍会绕过。此次只补齐这些已确认缺口，保留已修好的普通文本、日期、radio、React/Vue、观测器与导师电话负词。

用户本轮只要求审查/计划，审查者未实施业务修改。本计划交给实施者后执行。不得读取真实高校账号/Cookie、访问认证站点、改竞品、自动提交/发布、增加自动保存/下一步/上传。保持完整一键填充、picker人工接管、14行动态表。不改成整页不填来通过负例。

新账本：`docs/dev-notes/implementation-progress-v5.md`。每卡最多一个聚焦改动批次；必要时分a/b，不机械限制行数。普通本地工作持续执行，不逐卡要求用户授权。

顺序：`G00 → G01 → G02 → G03 → G04 → G05 → G06 → G07 → G08 → G09`。

## G00 冻结反例和纠正验收状态

文件：新账本、回归入口、审查证据；不先改业务来使失败不可见。

1. 阅读本次报告和当前git diff；记录源码内容哈希，不能只写“以git diff为准”。不覆盖v3/v4历史，追加复审纠正。
2. 将 `ds-v4-review-2026-09-09/audit-probes-v4.cjs` 中V01–V07迁移为自动测试。Q01–Q10的已修部分保持回归，不把诊断脚本的exit0当pass。
3. V06目前是机制级示例，后续G04必须补真实生产回调/浏览器测试；不要把该示例写成ext证据。
4. 新增整链测试入口，必须执行实际合同→通用→合并，最好复用正式导出的编排入口；不得只测contract/helper然后覆盖矩阵写“生产已保护”。
5. 保留shouldWrite正例；失败暂归当前G卡，不得最终移到DEFECT豁免。

验收：能稳定复现V01重复、V02绕过、V03覆盖、V04依赖未阻塞、V05误恢复、V07提前收口。测试缺业务修复时应失败，检测工具自检通过。明确初始失败不是G00未完成，而是已成功建立基线。

## G01 共享目标身份、阻塞与结果合并

对应F01/F03。文件：`fill-task.ts`、`fill-merge.ts`、`task-compiler.ts`、`control-drivers.ts`、`content/index.ts`。

实施步骤：

1. 在内存中建立最小目标registry：run/document/targetId、候选节点集合、负责驱动、结果状态。可以复用已有类型，禁止再建第二套不被消费的类型。
2. 合同解析返回明确的无匹配/已绑定/歧义/阻塞结果，不能把歧义返回空数组后当“没有合同”。歧义候选节点进入本轮禁止写集合；明确属于该合同的后续通用路径不得绕过。
3. 仅阻塞已确认的歧义目标，独立通用字段仍填写。整页合同无法确定时记录原因，不能给未知页面随意赋予合同写入权。
4. label[for]目标也检查唯一性；标签容器有多个input/select时不能querySelector取首。radio组按name+form/实际作用域区分，避免不同表单同name被合并。
5. 合同filled/failed/conflict/alreadyCorrect/pending分别进入唯一结果；通用exclude产生的内部跳过不另算用户目标。缺少el的合同缺失/阻塞仍保留逻辑目标结果。
6. 代码/名称/显示三载体与表格目标定义主任务及子载体，不将同一任务计算三次。总数由registry目标集合生成，不是数组拼接长度。

验收：V01一个空合同姓名total=1、只有一个outcome；V02两个歧义候选均不写，独立邮箱照常；合同缺失可见失败；旧合工大noMatch→picker兜底仍可执行；同name不同form不会串组选中。

## G02 统一实际写前门禁与所有权

对应F02/F03。文件：`value-semantics.ts`、实际合同/通用/组件/表格driver、`filler.ts`。

实施步骤：

1. 列出所有实际setter、checked/select操作和组件点选入口。写前判定基于控件及字段语义，不因driver名为school-picker就绕过select保护。
2. 将通用和合同统一为empty/equivalent/conflict/unknown决策，结果使用G01状态，不用skipped混淆同值与冲突。代码/名称角色在G07进一步验收。
3. 在实际写入入口原子记录写前值、预期语义值、写后值、run/document、driver、用户干预标志；高亮markEl只负责UI，不能自动授予ownership。
4. 合同同样登记ownership与expectedValue。清除使用记录，不能漏清合同写入；用户原有同值不授予写入所有权。
5. 重复显式填写时，仅在记录仍有效且无干预时继承可清除信息。记录用户可信编辑或未知来源干预后失效，不能仅靠最终字符串再次相等恢复权限。
6. 资料长度和compose/manual等已有正确处理保留；专用表格不得在通用guard前任意写。

验收：V03原选项保持；每个受支持驱动空值必填/同值零setter及事件/不同值保留；V01合同写→清除正确；原有同值不清；用户改动后即使值又相同也不误清。保持已通过Q01/Q02/Q04/Q05/Q06。

## G03 所有driver的稳定验证和validation

对应F06。文件：执行器、合同/通用driver、content settle、浏览器夹具。

实施步骤：

1. 使用G02预期值与目标记录。实际写入后进入pending验证，成功有driver指定的比较/稳定条件、总deadline和可取消机制。不要继续增加420ms/15秒页面扫描补丁来替代执行器。
2. 可复用stableReadback，但先修其边界，再让生产调用；每种driver明确如何读值、名称、代码、模型，以及无法验证时的结果。不得跳过所有带widget/picker标记的目标却报告成功。
3. validation与值比较是两个维度：值等于目标，但出现本字段新增关联错误，仍不可成功。处理全部声明选择器；基线按目标与本轮保存，无关联/隐藏/既有错误不归因。
4. 验证期间节点被同id新节点替换、文档离开、轮次变化则取消或失效，不能悄悄continue留filled。catch不能保留成功状态而吞掉验证异常。
5. 保留已有React/Vue夹具，增加合同路径+受控框架/原生延迟清空组合，不仅测试localhost通用路径。所有框架资源本地提供。
6. 内存registry持续保留；序列化汇总不能替换本地元素/预期值。补填与首次写入走同一执行器，不能丢前一轮尚未验证的目标。

验收：本次真实扩展合同清空反例在最终明确failed/uncertain，total=3且姓名不报告成功；稳定合同正例成功；值保留但关联错误出现判失败；第二个错误选择器也生效；旧/隐藏/无关联错误不误判；取消后不挂起、不写；React/Vue正反例维持通过。

## G04 原轮快照与导航失效贯穿每个恢复点

对应F04。文件：`fill-session.ts`、content异步链、picker/行任务driver。

实施步骤：

1. 定义每轮不可变context并显式传递。`runStillActive(context)`必须比较原轮与当前轮，禁止无参guard只读取最新全局。
2. `FILL`在loadProfile前捕获context，Promise恢复后先核对；450/1200/2400ms、80ms picker、保存恢复900/2600/6000ms均捕获各自原轮。已有2.5/7/15秒快照正确部分保留。
3. picker/级联/动态行每次await后，在下一次DOM写/点击前检查原轮和目标连接。列出函数及恢复点清单，不能只写“复用F04守卫”。
4. 保存后恢复若需要新轮，只在明确恢复启动时创建并加载当前资料；失效的旧回调不得自己重生或沿用旧profile。
5. profile/规则/适配包修订均失效；生产RunSnapshot实际包含所需修订，不只在未消费接口里声明。
6. 路由使用已知页面ID和内存不透明身份；区分hash逻辑页与必要query页面语义。持久化键不含动态pathname令牌，也不存完整URL以绕过脱敏。

验收：A排队→B启动→A回调无副作用；读取档案期间启动B，A返回不冒充B；picker/加行await中换档案或导航不再写；保存恢复旧profile不复活；同URL新Document无串用；合成query/hash/path token均不落盘，同时合法路由变化能使旧轮失效。必须有实际扩展本地浏览器断言。

## G05 frame参与者与终态协议

对应F05/F10。文件：background、aggregation、content消息、DTO。

实施步骤：

1. 增加注册/启动确认/进度/终态的最小协议，key包含run、frame、文档实例和seq。说明启动窗口内参与者集合如何确定；可以由content主动注册，不因没有webNavigation就省掉协议。
2. 所有已确认参与者达到终态才正常完成；硬deadline只产生部分完成/超时，明确未响应参与者。固定1200ms不再充当成功条件，也不能只是延长到更大常数。
3. 420ms或后续driver验证结果更新后，所属frame必须上报新seq；顶层不能只用自己的filled/failed覆盖所有frame总数。
4. 缺run/document/seq的报告和FILL_DONE均拒绝；新旧文档同frame区分。旧run迟到失败不得修改新轮，当前run有效迟到更新按协议处理。
5. 收发两端白名单验证DTO；剥valuePreview/expectedValue/context/代码/URL等，reason/label也不能直接带资料值或原始错误HTML。汇总重新生成唯一目标统计。
6. 本地runtime registry与展示DTO分离，顶层可以展示总状态但不能丢失自身执行记录。

验收：V07不提前成功，迟到当前参与者失败被正确反映；0帧/1帧成功1帧超时/子frame延迟清空/多帧乱序/重复seq/旧文档均有浏览器或实际background入口测试；至少一个子frame执行超过1200ms；无终态的初始回报不算完成；DTO额外合成敏感字段无法转发。

## G06 恢复权限与真正恢复的变异测试

对应F07。文件：`conditionalRestore`、写入/失败执行器、恢复夹具。

实施步骤：

1. 缺ownership、缺原轮context、缺写前快照，任一个都notAttempted。before空字符串是有效值，undefined是缺失，绝不能混同。
2. 校验同run/document/target、连接状态、明确支持的可逆文本driver、当前仍等于最后自动写入状态、无用户/未知修改。禁止对日期、radio、文件等宽泛按INPUT恢复。
3. 区分“扩展实际恢复成功”与“页面已回原值，无需动作”。报告可以标alreadyRestored/noop，不能将其作为实际执行恢复的证据。
4. 在实际可归因失败路径调用恢复。例如允许写入但出现本字段关联validation错误，值仍为自动写值、所有权完整，恢复到写前空值；来源不明改值则保留。
5. 夹具断言真实恢复发生：在页面未自行清空条件下观测恢复写入/事件及终值。保留现有不可恢复夹具，但不能冒充可恢复证明。
6. 加入变异负向检验：暂时让conditionalRestore永远notAttempted，真正恢复测试必须失败；撤销变异后通过。变异仅在临时构建/测试副本，不留生产开关。

验收：V05保留USER_VALUE；脱离/跨run/缺所有权/用户改动/不可逆均不写；至少一次真实扩展主动恢复和一次不可恢复；关闭恢复逻辑会失败；过程零提交。输出恢复原因，不记录真实资料值。

## G07 依赖状态、picker角色与动态行

对应F08。分a/b实施，不能再用“有拓扑排序单测”结束。

G07a 依赖：

- 定义依赖满足条件为已验证成功或已验证同值；档案空、冲突、失败、未知、等待人工、取消都不能直接放行子任务。
- 所有状态经同一调度出口传播，避免遗漏continue分支；picker pending直到完成验证才释放依赖。依赖失败也进入G01禁止写集合，通用链不能补写。
- 新建本地省→市→区或学校→专业的异步浏览器夹具，父选择后延迟替换子控件；生产执行器等待真实变化并选对值。环/缺引用加载期拒绝，运行时也不兜底顺序写坏图。
- 验收：V04空/冲突阻塞子项，独立项照常；父同值允许合法子项；父延迟成功后才写子；取消/替换停止；环不写。仅jsdom同步字段顺序不满足本卡。

G07b picker/行任务：

- 在任何code比较前确认当前input到底是名称还是代码载体。当前先对input.value调用pickerCodeConflictDecision再做pair判定的顺序必须纠正。
- 读取完整的代码/名称/显示绑定；不能只取selectors[0]或者以“名称非空”代替名称正确性。namespace、括号、K/T后缀保持。
- 删除未经所有权证明的“看起来不是纯数字就清空”自愈路径；人工值冲突保留并给出字段级等待原因。
- 持久化摘要与内存执行值分离，刷新不伪造旧轮权限；人工跳过只影响本轮，新轮可重试。
- 行任务逐await使用G04context，半填行、加行失败、14行上限和禁止下一步持续验证。
- 验收：合法名称框不误判代码冲突、人工改码不清、等待picker后依赖正确继续、刷新恢复摘要不泄漏/不误写；合工大精确三联与北科14行通过。

## G08 完整合同试点与不可绕过的证据校验

对应F09。文件：check-adapters、evidence schema、ustb试点、浏览器断言。

1. 明确区分“通用行为夹具”与“完整合同试点”证据。generic可保留单独schema，但不能以`(offline-pilot)`前缀跳过合同验收。
2. 完整合同必需：真实packageId/version/pageId、生产配置内容哈希、非空样本file/hash、完整字段/载体策略清单、测试入口及实际通过结果。必需项缺失立刻失败，不optional跳过。
3. 版本和配置哈希与当前源码比对；字段清单与实际可写合同清单比较完整集合，不能只校验子集。所有shouldWrite/preserve/wait/forbidden均有明确归属。
4. offlinePassed=false不能进入通过证据集。liveVerified自动生成固定false，不能用随意非空reviewer字符串宣称真实人工验收；真实live仍不在本轮执行范围。
5. 加入每个篡改单独非零断言：缺file、缺hash、错hash、错版本、配置变更、字段缺项、多余字段、空清单、虚构包前缀、offline失败。
6. 北科日期断言真实期望年月，不是length≥6；同值三联断言零setter/事件，不仅终值相等。补空三联实际选择、不同值保留、延迟拒绝和禁止操作。样本简化范围如实说明。

验收：本次6个错误放行全部拒绝，正确证据通过；任改合同配置后旧证据失效；试点通过G01–G07新执行链。不能用修改JSON的offlinePassed=true代替运行。

## G09 最终报告、CI与独立复核

对应F10及其余v4未闭环要求。

1. 统计从唯一目标状态计算，并保证count与具体items一致；明确alreadyCorrect与实际写入次数的差别。顶层和子frame不会相互覆盖计数。
2. 完成提示只能来自G05终态；不得在picker/依赖/验证仍pending时出现“已回读通过”。冲突、失败、超时、取消均有具体字段及处理方式。
3. 保存证据补包版本/页面/表格身份和失效规则，保留30分钟期限；旧路由/旧配置/旧表格证据不能放行。不回退到按钮文字判已填。
4. CI纳入新硬断言和本地浏览器关键回归；现有仅unit/jsdom的workflow不会保护React/Vue/跨frame场景。CI浏览器用实际可在CI安装的Chromium，不能依赖Windows Edge固定路径。
5. CI安装可联网，业务测试本地路由并拦截未声明请求。远端未运行如实说明，不因用户未要求发布而擅自push。
6. 覆盖矩阵为每个R/Q/V/G列具体测试名称、生产入口、证明层级、最新结果与源码哈希。代码级检查不写ext；恢复页面自己回原值不写实际restore。

最终门禁（PowerShell）：

```powershell
npm run typecheck
npm test
npm run check:adapters
npm run test:regression
$env:PW_HEADLESS = '1'
npm run test:e2e
npm run test:regression:e2e
git diff --check
```

G卡新增用例必须接入以上入口或补充明确的package script并进入最终门禁。负向自检包括观测器违规、恢复禁用、证据篡改，预期非零；结束前撤销所有临时变异并再次确认正常构建通过。不要运行test:e2e:real。

## 1. 进度账本与完成声明

每卡保存：状态、源码hash、真实入口、改动文件、正例/反例、命令退出码、日志、Remaining、下一步。passed前检查Remaining是否含本卡强制项；若包含只能in_progress/failed，不能写“passed主体”。

最终完成需要：G00–G09全部强制验收满足；本次V反例关闭；v3/v4未取消的R/Q场景补齐；完整一键填充能力不退化。真实高校live保持未验证，远端CI状态单列，不与本地完成混淆。

禁止在强制项未做时再输出“全部passed，只有这些剩余”。若未完成，标题写“整改未完成”，列出具体卡和缺失证据。换对话从本账本及当前代码继续，不依据上一段AI总结跳过验收。
