# DS v7 最终报告(2026-09-10)

项目:`D:\deepseek-work\tuimian-autofill`。执行者:DeepSeek(接手)。依据:`DeepSeek-V4-Flash-整改PLAN-v7.md`、`DS-v6复审与直接修复报告-2026-09-10.md`、`codex-v6-handoff-2026-09-10.md`。

## 结论:整改完成(I00–I03 强制项已满足)

四张卡的强制验收项均已实测满足;9 条门禁全绿(`--negative-sync`、`--ignore-cancellation` 按预期非零);本轮新增的取消/门禁/脱敏断言均经变异自检证明承重。证明层级的保留项在第五节列明,不静默。

## 一、逐卡结果

| 卡 | 状态 | 关键改动 | 关键证据 |
|---|---|---|---|
| I00 | passed | 完成轮回执(晚注册可见/去重/有界)、终态由工作集合归零驱动、终态即最终语义 | `v7-lifecycle` 9 项 + ext 精确计数 `total=5`;门禁变异 → 1 |
| I01 | passed | BlueFlat 7 处取消、widget 3 处取消、动态表 matchExisting/dialog/行内提交/blob 复核、所有权清单 | `driver-cancellation-e2e` 22 场景(原 17 + 5);两族各自变异 → 1 |
| I02 | passed | reason 固定模板、标签短名单、摘要/站点扫描/调试块清洗、报告去页面原文 | `v7-telemetry` 6 项 + ext 合成标记零落盘;变异 → 1 |
| I03 | passed | v7 账本/矩阵、README 能力边界、历史文档标注、证据 hash 绑定本轮源码 | 本报告 + 门禁日志 |

## 二、关键实现要点

- **I00 生命周期**:background 保留"完成轮回执"(TTL 60s、容量 8 轮、每轮最多 32 个晚注册区域)。收口后到达的注册得到 `run-completed/covered:false` 回执,该区域与顶层分别收到明确告知;重复晚注册按新区域去重计数;未知轮拒绝。content 侧终态只在该帧"settle 待验证 + 级联下拉 + picker/行任务/日期组件/补填"全部归零后发送(250ms×40 有界),不再用固定 15s+470ms 代替工作结束;`accept()` 拒绝终态后的更高 seq,与"收口即删轮"统一。
- **I01 取消边界**:把原轮取消谓词贯穿到 BlueFlat(等待弹窗/查询/点选/回填/写代码/兜底)、jqx 组件下拉(主世界返回/轮询/过滤等待)、动态表(行内匹配/dialog/行内提交/blob)。行内提交仍只在 `allowCommitActions && maxAddAttempts>0` 且按钮为明确 DoPostback 加行动作时点击,不借"添加"扩大自动保存授权。
- **I02 脱敏**:持久化原因只允许问题码固定文案或登记模板;持久化标签只允许短纯文字字段名;摘要/站点扫描/调试块全部清洗;字段报告去掉页面标签与选项原文(选项只留数量);`profileLists`(档案内容快照)已从 sessionStorage 摘要移除。用户点「复制漏填项」被明确定义为"用户导出(含资料)"——只在内存生成、直接进剪贴板、不落盘。

## 三、最终门禁实测(日志:`docs/analysis/ds-v7-verify-2026-09-10/final-*.log`)

| 命令 | 退出码 |
|---|---|
| `npm run typecheck` | 0 |
| `npm test` | 0 |
| `npm run check:adapters` | 0 |
| `npm run test:regression` | 0(HARD 58 / FAIL 0) |
| `PW_HEADLESS=1 npm run test:e2e` | 0 |
| `PW_HEADLESS=1 npm run test:regression:e2e` | 0(含 I02 脱敏、I00 精确计数、H01/H03/H04、22 项 driver、8 项依赖) |
| `node test/dependency-e2e.mjs --negative-sync` | 1(预期) |
| `node test/driver-cancellation-e2e.mjs --ignore-cancellation` | 1(预期) |
| `git diff --check` | 0 |

负向自检(实测,撤销后均 0,无生产开关残留):H01 首项推断、H02 修订校验、行任务取消谓词、picker 取消谓词、终态工作集合门禁、BlueFlat 取消检查、widget 取消检查、遥测原文直落 —— 每一项移除后对应断言失败。

## 四、本轮修复的审查反例

C05(完成后才注册的 frame)→ 收口后回执/提示/去重/有界(bg);C06(遥测 reason 内容)→ 全出口脱敏 + 真实页面零标记(ext)。C01–C04、C07、C08 由 Codex 本轮直接修复,回归保持通过。

## 五、明确保留(不静默)

- `liveVerified=false`:未访问真实高校认证站点、未读 Cookie、未用真实账号。
- 22 项 driver 场景使用合成 DOM 协议,不等于安装了真实 Ant/Element/select2/layui/jqx 框架,也不等于真实高校页面。
- `旧文档/乱序 seq/重复 seq/超时/等待人工` 为真实 background 入口(bg)证据,非浏览器内——浏览器无法伪造帧级乱序消息,任务书亦禁止给正式扩展增加测试接口。
- `.github/workflows/offline-gates.yml` 未在真实 CI 运行;未提交、未 push。
- 未修改竞品目录、未删除用户未跟踪文件;未新增自动保存/下一步/提交/上传;picker 人工接管与 14 行动态表保持。

## 六、文档

| 文件 | 内容 |
|---|---|
| `docs/dev-notes/implementation-progress-v7.md` | v7 逐卡账本(改动/证据/退出码/源码哈希/保留项) |
| `docs/dev-notes/regression-coverage-v7.md` | 单一当前状态表 + 反例关闭表 + 证明层级边界 |
| `README.md`「能力边界与验证层级」 | 对外能力边界与诊断内容边界 |
| `docs/analysis/ds-v7-verify-2026-09-10/` | 本轮门禁日志 |
