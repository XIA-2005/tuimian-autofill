# DS v7 复审与接管报告

日期：2026-09-10。项目：`D:\deepseek-work\tuimian-autofill`。

**结论：DS提交时未达到“I00–I03强制项全部满足”。本轮已直接修复已复现的提前终态、终态后补填、多处脱敏和回执边界；取消/超时终止、人工接管续轮及旧诊断迁移仍需按PLAN v8完成。**

DS报告/账本是被审查材料，不是已完成证明。用户授权本轮审查及接手修复；本轮修改了源码和测试，未commit、push、发布、访问真实高校认证页面或Cookie。

## 1. 验证基线

修改前复制到 `C:\Users\m2694\AppData\Local\Temp\tuimian-v7-audit-20260910-131147`，独立构建、node_modules联接原项目。DS基线的build、typecheck、单元、58项回归、check-adapters、原有扩展E2E、扩展回归、8项依赖和22项driver场景均退出0。

证据目录：`docs/analysis/codex-v7-review-2026-09-10/`。包含基线日志、新反例脚本及JSON、修复后日志、旧代码运行新断言的负向日志、相对本轮基线的源码hash变化清单。

诊断脚本退出0表示成功采集结果；其中expected与actual不一致就是缺陷，不能把采集退出码当业务通过。

## 2. 新反例与处理结果

| 编号 | DS基线复现 | 本轮处理 |
|---|---|---|
| D01 ASCII诊断内容 | `sanitizeDiagnosticValue('ASCII_PRIVATE_TOKEN')`原样返回 | 已修：仅固定枚举/阶段码/已登记字段可保留，其他字符串只保留长度 |
| D02 短姓名标签 | “张三”被当作安全短标签原样保留 | 已修：固定字段词表与本地中文标签，不按长度判断隐私 |
| D03 扫描URL | PRIVATE_PATH和PRIVATE_QUERY原样进入扫描结果 | 已修：使用不透明逻辑路由；报告不再输出原始pathname/title |
| D04 对象键/摘要stats | PRIVATE_KEY、PRIVATE_VALUE、PRIVATE_STATS仍可出现在输出 | 已修：未知键替换、stats只保留登记计数，字段/状态/问题码验证 |
| D05 轮询次数用尽 | 真实函数在pending=true、attempt=40时仍发送终态，部分状态可报done | 已修：上限不代表完成，等待后续完成事件或后台超时 |
| D06 终态后继续写 | 子frame约9.8秒终态，15秒又写入专业；终态仍保留补填前两个失败 | 已修并增加完整扩展浏览器反例 |
| D07 回执边界 | 原参与者重复注册误报未覆盖；容量满后重复通知；活动轮重复注册重复计数 | 已修，真实background VM回归 |

### D06的浏览器证据

真实打包扩展、无账号临时浏览器、本地父/子frame夹具。仅将子frame选项出现时间从1.3秒改为12秒：

- 10.5秒：子专业仍为空，但已经收到该frame的waiting-manual终态，失败数2。
- 17秒：子专业已是“软件工程”，没有新的终态；顶层仍保留失败数2。
- 未发生外联。

说明“终态即最终”只在聚合器禁止更新时成立，生产回调仍在终态之后写值，数据和终态已经分离。不是仅有测试层级不足。

新增正式浏览器断言要求：10.5秒顶层仅展示自身1项；15秒补填并验证后汇总5项，失败数为1。修复后通过。

## 3. 本轮直接实现

### 运行与结果

- `frameWorkPending`按原轮检查settle，并记录实际异步填写和日期Promise。
- 轮询达到上限不再发送伪终态；任务完成事件会重新触发收口检查。
- 终态已经发送后，尚未执行的2.5/7/15秒补填回调直接退出。
- 日期二阶段返回后更新同一结果对象的状态、计数和预期值，再登记稳定回读。
- 明确人工接管且没有其他自动工作时仍报告waiting-manual，不一律拖成超时。
- 仍未把后台deadline转换为全链中止，也未定义waiting-manual之后的续轮协议；这两项保留在v8。

### 脱敏与诊断可用性

- 删除“短文字安全”和“ASCII安全”假设。
- 固定字段ID/中文标签、状态枚举、问题码、BlueFlat固定阶段码和计数仍保留；未知页面字符串、属性值、对象键不原样输出。
- 各debug写入点在落sessionStorage前清洗，不再只在复制报告时清洗。
- 站点扫描不输出完整URL；报告去页面title及按钮/控件属性原文；widget探针与REPORT接收端也经过清洗。
- 修正脱敏后显示内部路径的问题：姓名、手机号、邮箱、学校/专业等使用本地固定中文标签，保留原UI可读性断言。
- 新测试分别输入Alice、张三、独立ASCII令牌、对象键和URL，避免所有标记混在中文串里导致假阴性。
- 实际扩展LZU形状夹具额外带ASCII查询令牌，验证遥测/摘要/站点扫描均不保留该令牌。

诊断现在是有界结构摘要，不保证保留全部原始CSS类名/页面文字。固定阶段码不被删除，原BlueFlat诊断测试仍成立。

### 回执

- 记录有界的已覆盖区域摘要，原参与者重发注册返回already-completed，不误报未覆盖。
- 活动轮late计数按新区域去重。
- 完成轮晚注册容量满后返回明确capacity回执，不因未存入Set而反复通知。
- 有另一个活动轮时明确拒绝旧run注册，不再静默返回。

## 4. 测试结果与边界

新增单元/VM断言通过；把新隐私和回执测试交给未修的DS基线，分别退出1。修复后的原有单元、58项回归、适配检查、扩展E2E、扩展回归（含12秒延迟新场景）、8项依赖、22项driver保持通过；负向driver/依赖自检仍退出1。

构建一次后执行实际runner，避免并发重建。最后涉及运行时逻辑的改动已复跑对应扩展回归；具体日志和退出码见接管账本。

22项driver是浏览器执行生产driver的合成DOM协议；VM承担消息顺序/回执故障注入。这样的证明层级本身是合理的，不要求伪造真实站点验证，也不需要给正式扩展增加测试接口。

## 5. 剩余问题不是新的产品需求

1. 后台超时不会使content原轮失效；改档案虽然能停止许多写入，但没有对应的及时取消终态。取消、超时和UI提示还未统一。
2. waiting-manual已经是不可更新终态，但人工完成后的原轮回调仍可能继续；需要明确暂停状态或新续轮，不能既冻结结果又在旧轮继续写。
3. 新写入出口已加固，旧持久化遥测的加载/迁移仍需校验，不能把旧原文重新带回当前日志。

下一步只执行PLAN v8 J00–J02，保留本轮修复，不重复重做G07a和已经通过的driver场景。整体仍未达到最终完成；liveVerified=false、远端CI未运行、未发布。
