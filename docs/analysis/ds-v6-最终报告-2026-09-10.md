# DS 接手 v6 最终报告(2026-09-10)

项目:`D:\deepseek-work\tuimian-autofill`。执行者:DeepSeek(接手方)。依据:`DeepSeek-V4-Flash-整改PLAN-v6.md`、`DS-v5复审与接管结果-2026-09-10.md`、`codex-g07a-handoff-2026-09-10.md`。

## 结论:整改完成(H00–H05 强制项已满足)

六张卡的强制验收项均已实测满足;9 条门禁全绿(`--negative-sync` 按预期非零),新增的 H01/H03/H04 浏览器断言均经变异自检证明承重。证明层级的保留项见第五节,不静默。

## 一、接手前独立复核(不采信任何一方"完成"结论)

8 条门禁独立复跑,全部符合预期;`--negative-sync` 退出 1(证明"退回同步执行"时依赖场景必失败)。G07a 逐条对照 PLAN v5 验收:异步夹具、父延迟成功后才写子、取消/替换停止、环不写、V04 空/冲突阻塞、父同值放行——均已覆盖。如实记录:`dependencyWait` 仅由测试契约声明,真实生产包(LZU)只声明 `dependsOn` 并走默认 3500ms/100ms。

## 二、逐卡结果

| 卡 | 状态 | 关键改动 | 关键证据 |
|---|---|---|---|
| H00 | passed | 复审1–7 转硬断言;同步入口遇依赖页只返回待异步处理 | `v6-audit` 全绿;H01 变异 → 1 |
| H01 | passed | 删除"首项=空"推断;label[for] 重复 id 全歧义;标签容器多控件不取首;radio 无 name 不合成组 | 7 条 unit + ext「H01 首项保护」;变异 → 1 |
| H02 | passed | 写入记录绑定 runId/代际/修订;恢复必须传原轮 ctx;真实 input.type 非文本不恢复 | 5 条 unit + G06 ext;变异 → 1 |
| H03 | passed | 随机文档身份、注册封口 700ms、deadline 25s、终态类别 done/waiting-manual/cancelled、未响应/晚注册面板可见 | 9 条 unit/bg + ext「H03 跨 frame 终态协议」 |
| H04 | passed | 行任务/picker 取消谓词贯穿 await 恢复点;多阶段 settle 并存;人工接管后重新验证继续;逐 driver 策略表;错误归因词边界匹配 | 8 条 unit/contract + 2 条 ext;两条 ext 均经变异 → 1 |
| H05 | passed | DTO 双向固定标签、reason 不跨上下文;证据哈希覆盖完整字段契约 | 2 条 unit;六项篡改仍各 1 |

## 三、本轮新增的实现要点

- **H03 终态协议**:`FILL_TERMINAL` 带 `terminalKind`;background 按参与者记录并在 `FILL_DONE` 输出 `terminalKinds`/`missing`/`lateRegistrations`;新轮替换旧轮时向旧调用方显式返回 `cancelled`。
- **H04 取消贯穿**:`runDynamicTableFill` 与 5 个行填充入口、`runMinimalPicker`/`pickSchool`/`pickMajor`/`pickInPage` 均接受 `isCancelled` 谓词,在等待弹窗、每轮查询前、点击结果行前、写行前与网格等待后复核原轮;content 传入 `() => !runStillActive(ctx)`。
- **H04 settle 并存**:新增 `SettleRegistry`,同一轮内多个补填阶段的 pending 验证并存、收口一次性取回,不再被"只保留最新 settleRun"覆盖。

## 四、最终门禁实测(日志:`docs/analysis/ds-v6-result-2026-09-10/`)

| 命令 | 退出码 |
|---|---|
| `npm run typecheck` | 0 |
| `npm test` | 0(v6-audit / v5-audit / 两个异步套件全绿) |
| `npm run check:adapters` | 0 |
| `npm run test:regression` | 0(HARD 58 / FAIL 0) |
| `PW_HEADLESS=1 npm run test:e2e` | 0 |
| `PW_HEADLESS=1 npm run test:regression:e2e` | 0(含 H01/H03/H04 新浏览器断言 + 8 依赖场景) |
| `node test/dependency-e2e.mjs` | 0(8 场景) |
| `node test/dependency-e2e.mjs --negative-sync` | 1(预期失败) |
| `git diff --check` | 0 |

负向自检(实测,撤销后均 0,无生产残留):H01 首项推断恢复 → `npm test` 1;H02 修订校验禁用 → 1;行任务取消谓词移除 → `test:regression:e2e` 1(rows=6 全部写完);picker 取消谓词移除 → 1(出现 choose 且代码/名称被写)。

## 五、明确保留(不静默)

- 证明层级:H03 的"旧文档回报/乱序 seq/重复 seq"由**真实 background bundle 消息入口**在 VM 中验证(bg),非浏览器内——浏览器无法伪造帧级乱序消息,而任务书禁止给正式扩展增加测试契约消息接口。H04 的组件下拉(layui/ant/select2/element)、日期面板、行内提交按钮的取消路径只有既有单测/契约测试,未做浏览器断言(不宣称全覆盖)。
- `liveVerified=false`:未访问真实高校认证站点、未读 Cookie、未用真实账号。
- `.github/workflows/offline-gates.yml` 未在真实 CI 运行;未提交、未 push。
- 未修改竞品目录、未删除用户未跟踪文件;未新增自动保存/下一步/提交/上传;picker 人工接管与 14 行动态表保持。

## 六、下一步建议

1. 若需浏览器级"旧文档/乱序 seq"证据,需要引入仅测试构建的帧消息注入口(当前任务书明确禁止正式扩展加测试接口,需先确认边界);
2. 组件/日期驱动的取消路径可按同一 `isCancelled` 模式补齐并加浏览器夹具;
3. 远端 CI 实跑与 live 验收需用户另行授权。
