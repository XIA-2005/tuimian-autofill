# 复制给 DeepSeek 执行

请在 `D:\deepseek-work\tuimian-autofill` 继续整改。先读取：

1. `docs/analysis/DS-v4完成情况复审-2026-09-09.md`
2. `docs/analysis/DeepSeek-V4-Flash-整改PLAN-v5.md`
3. v4计划、当前git diff，以及v5账本（若存在）。

“F00–F10全部passed”已被新的生产模块和真实扩展反例否定。保留已修好的通用文本、日期、radio、React/Vue和观测器；此次重点修合同与通用之间的阻塞/所有权/统计、合同稳定验证、原轮异步快照、frame终态协议、真实恢复、依赖与严格试点证据。

按G00→G09连续执行，逐卡写`docs/dev-notes/implementation-progress-v5.md`。必须把V01–V07和证据篡改用例变成硬断言；合同helper拒绝不等于通用整链不写；页面自己清空不等于扩展执行恢复。禁用恢复时真正恢复测试必须失败。强制项没做不能写passed，也不能移动到需要另行授权的backlog。

保持完整一键填充、picker人工接管和14行动态表；不增加自动保存/下一步/提交，不访问真实高校账号，不读Cookie，不改竞品，不删未跟踪文件，不自动提交或发布。本地浏览器夹具属于当前整改工作，持续执行无需逐卡询问。

最终报告只陈述实测生产行为、命令退出码和证据。所有强制项满足才写完成，否则写“整改未完成”并指出具体卡。liveVerified保持false；远端CI未运行则单独如实说明。
