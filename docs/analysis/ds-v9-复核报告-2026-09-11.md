# DS v9 交接复核报告(2026-09-11)

> 后续状态：本报告记录 00:38–00:53 的历史复核过程。其“当前工作区需冻结后复跑”结论已由 `DS-v9复审与直接收口报告-2026-09-11.md` 关闭；不要再把当时的并发进程和 133/135 状态当作当前事实。

项目:`D:\deepseek-work\tuimian-autofill`。执行者:DeepSeek(复核方)。依据:`DeepSeek-V4-Flash-整改与复核PLAN-v9.md`(K04)、`DS-v8复审与直接修复报告-2026-09-11.md`、`docs/dev-notes/codex-v8-handoff-2026-09-11.md`。

## 结论:本轮离线修复复核通过(针对 00:38 冻结态),但当前工作区已被并发会话再次改动,需冻结后复跑一次门禁

在**哈希冻结态**(与 manifest 修复后清单 135/135 一致)上,K04 五项要求逐条实测通过:8 条门禁全 0、5 项正向独立入口全 0、4 条负向自检全部退出 1;我自备的独立探针 28 条断言全绿(该探针跑在**改动之后**的 `fill-merge.ts` 上,同样全绿)。**未发现新反例,因此本轮未修改生产代码。**

> **重要限制(详见第十节)**:复核期间另一个会话在 00:49:39 又改了 2 个源文件,并在 00:53:03 启动 `npm run test:e2e`。因此"8 条门禁全 0"只对**改动前**的冻结态成立;当前工作区的门禁尚未复跑。

## 一、K04.1 工作区哈希核对(不依赖旧 HEAD)

用修复前后对照清单 `docs/analysis/codex-v8-review-2026-09-10/source-change-manifest.json`(135 条目)逐文件比对**当前工作区**内容:

| 指标 | 结果 |
|---|---|
| 与"修复后哈希"一致 | **135 / 135** |
| 与"修复前(v8 原样)"一致 | 0 |
| 第三次改动 | 0 |
| 文件缺失 | 0 |

→ 当前工作区确认就是 v9 修复后的状态,修复之后没有被任何人再改。工具:`tools/verify-v9-hashes.cjs`(可复跑,漂移时退出 1)。

本轮被改动的 13 个文件:aggregation.ts、background/index.ts、content/index.ts、fill-merge.ts、fill-telemetry.ts、v8-lifecycle.test.ts、v9-protocol.test.ts、v9-review.test.ts、test/run.ts、deadline-stop-e2e.mjs、manual-handoff-e2e.mjs、package.json、test/.v9-tmp-run.mjs。

## 二、K04.2 门禁实测(日志 `docs/analysis/ds-v9-verify-2026-09-11/g-*.log`)

| 命令 | 退出码 |
|---|---|
| `npm run typecheck` | 0 |
| `npm test` | 0(含 v9-review / v9-protocol / v8-lifecycle / v8-migration) |
| `npm run check:adapters` | 0 |
| `npm run test:regression` | 0(HARD 58 / FAIL 0) |
| `PW_HEADLESS=1 npm run test:e2e` | 0 |
| `PW_HEADLESS=1 npm run test:regression:e2e` | 0 |
| `git diff --check` | 0 |

## 三、K04.3 正向独立定位(p-*.log)

| 入口 | 退出码 | 场景数 |
|---|---|---|
| `node test/dependency-e2e.mjs` | 0 | 8 |
| `node test/driver-cancellation-e2e.mjs` | 0 | 22 |
| `node test/deadline-stop-e2e.mjs` | 0 | 1(缩短 deadline 的测试专用构建) |
| `node test/manual-handoff-e2e.mjs` | 0 | 1(实际等待 FILL_PAUSE 后再人工回填) |
| `node test/manual-handoff-e2e.mjs --reject-resume` | 0 | 1(后台拒绝后子项为空、人工值保留) |

## 四、K04.4 负向自检(n-*.log,均退出 1)

| 命令 | 退出码 | 失败形态 |
|---|---|---|
| `manual-handoff-e2e.mjs --ignore-resume` | 1 | **超时型**,落在第 144 行"子项填充"断言(位置正确,非偶发) |
| `deadline-stop-e2e.mjs --ignore-deadline-stop` | 1 | 断言型:`J00: 超时停止后不得写入迟到选项` |
| `dependency-e2e.mjs --negative-sync` | 1 | **超时型**(同步退化后子控件不被填) |
| `driver-cancellation-e2e.mjs --ignore-cancellation` | 1 | 断言型:`ant/cancel: 取消/移除后不得搜索或选择` |

如实说明:其中两条是超时型失败而非断言型(证据强度略弱),我已核对超时发生在**目标断言那一行**,不是跑得慢或环境抖动。

## 五、自备探针独立复验(不复用 Codex 的测试)

`docs/analysis/ds-v9-verify-2026-09-11/independent-probes.cjs`——断言全部从 PLAN v9 K00–K02 的需求原文重新推导,28 条全绿:

| 组 | 覆盖 |
|---|---|
| K00 序号 | 暂停缺 seq / seq=Infinity 被拒(不补默认 1);合法暂停接受;旧恢复不解除新暂停;新恢复接受;旧暂停不覆盖新恢复;**恢复缺 seq / Infinity 同样被拒(姊妹消息守卫一致性)** |
| K00 预算 | 仅人工暂停时启用 5 分钟预算、无 25s deadline;新自动区域加入后**恢复 25s deadline**、暂停预算撤销 |
| K00 到期 | 未完成区域只计一次;保留暂停前已回报快照(total 3 / filled 2);waiting-manual 只计一次;STOP 带 runId + docId;reason=manual-timeout |
| K00 旧广播 | 旧轮广播迟到失败后,**新轮聚合器仍正常收口**(runId 正确、total 正确) |
| K01 合并 | 同控件旧 filled + 新 failed → 保留 failed;旧 filled + 新 picker → 保留 picker;未重新确认的旧目标不沿用成功;合并后 stats 与 items 同源一致 |
| K02 迁移 | 旧 runId 不原文透传且重编码后仍可用于恢复;未来时间 / 负时间 / 起止倒置均拒绝;合法旧诊断保留计数与字段 |

另核对了两处"载荷对了但实现未必用"的条款(content 侧):
- `FILL_STOP` 在 content 里用 `msg.docId !== documentIdentity(document)` 核对当前文档(第 2466 行),**同 frame 旧文档的停止不会作用于新文档**;
- 人工到期与超时使用**不同文案**(`人工接管等待已到期` vs `填写超时`),未把正常等待冒称"页面失联"。

## 六、K03 测试可信度抽查(两条)

- v8-lifecycle 的旧轮消息已改为正确发送 `runId: firstRun`(不再是错误的 `firstRun` 属性),旧轮操作确实进入生产入口。
- deadline 与 manual 的变异锚点在替换前都断言唯一命中(`assert.equal(contents.split(anchor).length - 1, 1, '必须命中唯一生产锚点')`)——这正是我上一轮踩过的坑(源码改过之后旧变异串静默失效、负向假通过),现在会被显式拦下。

## 七、发现的问题(非阻塞,未改代码)

1. **临时文件残留**:`test/.v9-tmp-bundle.cjs`(155 KB)、`test/.v9-tmp-run.mjs` 是上一轮工具临时产物,未被 git 跟踪。属用户未跟踪文件,我**未删除**,建议你确认后清理。
2. 两条负向自检为超时型失败(见第四节),建议后续把它们的等待改成就地断言,让失败原因更直白。

## 八、证明边界(不夸大)

- 22 项 driver 为**合成 DOM 协议**,不等于安装了真实 Ant/Element/select2/layui/jqx;
- 协议序/暂停预算为**真实 background 入口 VM**;deadline 与人工恢复为**测试专用构建**(缩短 deadline、注入合成合同);
- `liveVerified=false`:未访问真实高校认证站点、未读 Cookie、未用真实账号;
- 远端 CI 未运行;未提交、未 push;未修改竞品、未删除用户未跟踪文件;完整一键填充 / picker 人工接管 / 14 行动态表保持。

## 九、并发改动与证据有效期(必须如实记录)

复核过程中检测到另一个会话正在同一棵工作树里工作,时间线如下:

| 时刻 | 事件 |
|---|---|
| 00:38 | 哈希核对 #1:**135/135 与修复后清单一致**(零漂移) |
| 00:39:39 – 00:49:19 | 我的 8 条门禁 + 5 正向 + 4 负向全部跑完(**均在冻结态上**) |
| **00:49:39** | 另一会话修改 `src/core/fill-merge.ts` 与 `src/core/v9-review.test.ts` |
| 00:50:37 | 我的独立探针 28 条(跑在**改动后**的 fill-merge 上)→ 仍全绿 |
| 00:51:29 | 哈希核对 #2:**133/135**,抓到 2 处"第三次改动" |
| 00:53:03 | 该会话启动 `npm run test:e2e`(复核结束时仍在运行) |

由此得出各条证据的有效期,不夸大:

- **有效**:8 条门禁、5 项正向独立入口、4 条负向自检 —— 全部在 135/135 冻结态上完成,该状态自 00:38 持续到 00:49:19。
- **覆盖新状态**:我的 28 条独立探针在改动之后运行并通过,即 `fill-merge.ts` 的新版本在我这 4 条 K01 合并断言下仍然成立。
- **未覆盖**:改动后的 `fill-merge.ts` **没有**重跑 8 条门禁;`v9-review.test.ts` 的新版本我也没有复核(我的探针不依赖它)。
- 另发现:**僵尸进程** `node test/.v9-tmp-run.mjs`(PID 28312,00:23:01 启动,至今约 30 分钟未退出),即 `test/.v9-tmp-*.mjs` 残留的来源。属另一会话的进程,我**未终止**。

**建议**:等该会话结束、工作树静止后,冻结一个 hash,再跑一次 `tools/verify-v9-hashes.cjs` + 8 条门禁,即可把复核升级为对当前态的通过结论。并行跑门禁会互相覆盖 `dist/`,因此不要与它同时跑。

## 十、本轮我新增的文件(均非生产代码)

- `tools/verify-v9-hashes.cjs`(哈希核对工具,可复跑)
- `docs/analysis/ds-v9-verify-2026-09-11/`(探针 + 全部门禁日志)
- 本报告

未改任何 `src/` 或 `test/` 文件,未新增任务卡,未扩大修改范围。
