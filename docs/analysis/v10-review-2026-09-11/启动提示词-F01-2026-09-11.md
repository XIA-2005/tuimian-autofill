# 启动提示词 · v10 阶段 F01（含 F03/F04 解冻补齐）

> 交给执行者（QWEN）。可直接作为新会话首条消息使用。**自包含**——不依赖上一会话的上下文。

---

## 0. 你的角色与硬边界

你是 **作者 + 执行者（单写者）**：唯一可写 `src/`、`test/`、`tools/`、`docs/`、`.github/`。
审查者是 **DeepSeek**：只读 + 自备探针（探针落 `docs/analysis/v10-review-*/`），禁改生产代码，**不复用你的测试**。
争议一律由**复跑事实**裁定：命令退出码、bench 四元组、`git diff`。**"通过"不可复跑即视为未交付。**

**红线（RD-1..RD-8，任务书 §5）不得触碰。**摘要：不改/不复制 `D:\巨能填系统-1.8.9`（构建输入路径含"巨能填"即 abort）；不自动保存/下一步/申报提交（唯一豁免 RD-C2 登录表单四条件）；不自动上传附件；不存账号密码、不读 Cookie 值；不引入 LLM；页面原文与资料值不落盘；禁虚假百分比对比；RD-7 哈希基线**只在审查通过后** update。

---

## 1. 接手第一步（不许跳过）

```bash
git log --oneline -6                      # 期望 HEAD = a886a3f 且与远端同步
git status --short                        # 期望仅有既存 5 个 M + 2 个 ??（.deploy-xiaaaaa-v203/、nul）
sha256sum "docs/analysis/任务书-v10-正确率与范围收敛-2026-09-11.md"
#   期望 a5f1f78c5f370155f348e31c8c57231ddc6a85a9af0e5e7286f56cb1fadc9f72
node tools/v10-hashes.cjs check           # 期望 一致131|漂移3|缺失1，exit 1（RD-7 预期审计态）
```

必读（按序）：`docs/analysis/任务书-v10-正确率与范围收敛-2026-09-11.md`（v10.1 冻结）→ `docs/analysis/v10-review-2026-09-11/预审意见-任务书-v10.md`（预审）→ **`docs/analysis/v10-review-2026-09-11/复审-F00F03F04F05-2026-09-11.md`（本次复审，你的第一优先）** → `docs/dev-notes/v10-execution-ledger.md` → `docs/dev-notes/v10-影响面-F05.md`。

**审查者已独立复跑并确认的事实**（不要再推翻，除非你有相反实测）：
`npm test` = 0 且含 `PASS: [v9-protocol] 83 项`；CI run `34564989235` @ `f49c93a` = success；临时 runner 已删；`.gitattributes` 已提交。

---

## 2. 第一件事：解冻补齐（F03/F04 未过审，不许签名）

审查者对 4 卡的判定：**F00 通过、F05 通过、F03 修订、F04 修订。** 差两项阻塞（详情见复审 §3/§4），补完我复跑通过才能 `v10-hashes update F03/F04`。

### B-1 · F04 哈希工具对"基线外新增文件"零覆盖（阻塞）

问题：`check()` 只遍历 `baseline ∪ signatures`，上报 `drift`/`missing`，**从不枚举"工作区存在但未签名"的文件**。实证：F03 新增并已提交的 `.gitattributes`（且已在其 touch-list 内）在 check 输出里**完全不被提及**。

危害：RD-7 实际只保证"已知文件集的漂移可知"。A1 将新增 `src/core/event-policy.ts` + `event-policy.test.ts`，A7 将新增 `moe-school-codes.ts` + `moe-major-codes.ts`——**漏声明漏签名时 check 仍报全绿**，任何卡都能新增任意源文件而不被发现。

要求：
1. `check` 增加 `UNSIGNED-NEW <path>` 枚举：受管根 = `src/`、`test/`、`tools/`、`.github/`、`docs/analysis/`、根配置文件（含 `.gitattributes`）；
2. 判据：每个 `UNSIGNED-NEW` **必须能在某张卡的 touch-list 中找到匹配**，否则 `exit 1`（"声明即允许、未声明即拒"，与既有 update 白名单同精神）；
3. 把 `.gitattributes` 纳入签名（当前是审计盲区）；
4. 负向自检：临时新增未声明的 `src/core/__probe.ts` → 必须 `UNSIGNED-NEW` 且 exit 1；删除后复绿。记录命令与退出码。

### B-2 · F03 的 e2e 修复缺负向自检 + 30s 超时未证因果（阻塞）

问题：`test/run.ts` 接线的负向自检有记录，但 `test/playwright.mjs` / `test/regression/run-e2e.ts` 的 `channel:'chromium'` 回退 + `15s→30s` **无任何负向自检**，而这正是 F03 里唯一改运行期行为的部分（§6 判据第 5 条未达）。且**超时加宽是让 flaky 变绿的经典手法**，账本未提供"channel 才是真因"的对照证据。

要求：
1. **因果对照**：临时移除 `channel:'chromium'` 回退（保留 30s），`PW_HEADLESS=1 npm run test:e2e` 必须**仍然失败**并具名 → 证明超时只是余量。记录命令/退出码，实验后还原。
2. **真负向自检**：`extensionPath` 指向空目录 → e2e 必须以具名断言非零退出 → 证明"绿灯 = 扩展真的加载了"，而非"永远等待"。`run-e2e.ts` 同款补 1 条。

### N-1～N-4（非阻塞，一并做，都是 1 行级）

- **N-1** F03 的"修 3 层 CI 红"是以"执行中实证的合法延伸"事后记账，改了 `playwright.mjs`/`run-e2e.ts` 运行期行为，超出 F03 卡面。**在任务书补一条"F03 范围变更记录"**，不要用事后记账代替改卡流程。
- **N-2** 提交数记述不符：你称 4 个，该区间实际 **5 个**（`30cb7f5`/`5958a4f`/`f49c93a`/`23b7e5f`/`a886a3f`）。账本类事实必须精确。
- **N-3** `.github/workflows/offline-gates.yml:3`"本工作流尚未在真实 CI 运行"**已被 `gh run view` 证伪**。F05 建议留到 D3——**改为立即修（1 行）**：治理文件里的假陈述本身即风险。
- **N-4** 任务书 §1 角色表把执行者写作"ZCode（本会话）"。审计链应记**被审查的代理标识（QWEN）**，否则跨模型交接无法区分"谁写的"。

---

## 3. 第二件事：F01 正确率基准设施

### 3.1 卡文要求（任务书 §3-F01，已吸收 P1–P9 全部，照做）

`test/bench/`，输入 = `{夹具页, 档案, 期望字段表(审查者冻结), 允许拒填清单}`；走 `src/core/fill-pipeline.ts` **真实入口**；四分类按 **P1 优先级**（`越界填 > 填错 > 漏填`，"拒填"为正交维度带理由）；越界按 **P4 台账口径**（`registerWriteOwnership` 每次调用 + 每次 DOM 赋值点，区分 attempted/committed，**不得按尾态**）；判等独立（**P5**：不得复用生产 `isSemanticEqual`/`readNativeControlValue`，bench 自带最小读取器）；报表脱敏（**P9**：只存计数/字段键/值形状正则/sha256，禁字段原文与页面原文）。

验收：`npm run bench` 退出 0 + 报表落盘；**越界填绝对 0**（非基线）；负向自检 `--negative-overfill` 注入一次期望外写入 → exit 1 且具名；`npm run bench` **并入 `test:offline`**（堵绕过路径 #6）。

### 3.2 审查者补充的前置要求（复审 §7，必须一并满足）

1. **oracle 覆盖率分层**：≥15 校夹具、每校 ≥20 期望字段，且每校覆盖字段须按**页面控件全集分层抽样**（必填/选填/拒填各占比），抽样规则写入 oracle 推导文档——防止"只挑好填的字段"。
2. **越界检测只读接入**：bench 读取 `registerWriteOwnership` 台账必须是**只读包装**，不得为 bench 修改生产登记逻辑（否则与 INV-A1-a 互相污染）。
3. **确定性**：同夹具同档案 → 同四元组。任何随机/时间依赖须固定 seed 与时间基准并写入报表。

### 3.3 oracle 提交协议（P6 + 复审 §6）

- 你先出草案 → `docs/analysis/v10-review-2026-09-11/oracle-draft-F01.json`；
- 每条期望 = `{夹具页, 控件定位, 期望字面值, precision, 依据}`，**`依据` 必须是外部可核物**（夹具 DOM 的 `name/id/label`、页面 `maxlength/pattern`、码表条目），**不接受"我认为应该填这个"**；
- 我复核"**推导链是否成立**"（不是"值好不好"），通过后签 `oracle-F01.signed.json`（含 sha256 + 判定行）；
- **oracle 全文将转第三方（GPT）做独立性审计** —— 因为 P6 让我签名 oracle 而我又判定后续卡，存在"我为自己的 oracle 当裁判"的悖论。你的草案必须**自足到可被外部审计**（依据列齐全）。

---

## 4. 每卡交付格式（7 条，缺一即记 failed）

文件:行号证据 ｜ 命令 + 退出码（**跳过/超时/未运行 ≠ 通过**）｜ bench 四元组（P1–P4 口径）｜ F02 对应红翻绿 ｜ **负向自检：具名断言 + 非零退出** ｜ `npm run test:offline` 全绿 + `git diff --check` 干净 ｜ 账本条目 + F04 重签 + 送审查者独立复跑。

**禁止**：改断言迁就失败；用"应该/理论"替代实测；把当前行为固化成期望（F02 纪律，红相位写**目标**绿行为）；事后补记流程（N-1 教训）。

---

## 5. 完成后交给审查者什么

一条消息给全：本卡 ID ｜ 改动文件清单（含新增）｜ 逐条验收命令与退出码 ｜ 负向自检的失败形态原文 ｜ bench 四元组前后 ｜ 账本条目路径 ｜ 需要我复跑的命令清单。**我不复用你的测试**——请把"我该跑什么、期望什么"写清楚。

---

## 6. 节奏

`B-1 → B-2 → N-1..N-4 →` 我复跑过审 → `v10-hashes update F03/F04` → `F01 实现` + `oracle 草案` → 我签 oracle → `F02`。

除阻塞外不必逐卡请示；**B-1/B-2 是闸门，未过不许 update 签名，也不许以"F01 已开工"绕开。**
