# 复审：GPT 独立补充审计（v10 阶段 F）

> **审查者**：DeepSeek（只读 + 自备探针）
> **被审对象**：GPT 输出（Edge 终验 / 竞品不可测 / oracle 不可审计 / D1–D3 未启动 / 覆盖数纠错）
> **日期**：2026-09-11 ｜ HEAD 仍为 `a886a3f`（**无新提交**）

---

## 0. 总结论

GPT 的**事实性工作基本可靠，我独立复现了它的关键项**；但它有 **1 项越权写盘**（违反 RD-7 单写者）与 **1 项数字需补证**。**F03/F04 的阻塞状态不因本轮而改变**——B-1/B-2 仍未修，`HEAD` 仍是 `a886a3f`。

| GPT 主张 | 我的独立复核 | 判定 |
|---|---|---|
| Edge `148.0.3967.54` headful `test:e2e` exit 0 | 版本**精确吻合**；我自己跑 `PW_HEADLESS=1 npm run test:e2e` → exit 0，且拿到同名两项采样 | **采信** |
| 竞品正确率当前不可测，依 P8 不产数字 | 结论正确且合规 | **采信** |
| 竞品 RD-1 未被破坏 | 竞品目录**逐项时间戳全停在 Sep 2**，`worker/browser_worker` 的 `package.json`(Aug 1)/`package-lock.json`(Jul 29)/`node_modules`(Sep 2) 均未动 | **采信**（但见 §3 纪律收紧） |
| oracle/bench 不存在，不得签名 | `oracle-draft-F01.json`、`oracle-F01.signed.json`、`test/bench/` 均不存在 | **采信** |
| 覆盖数 = 73 包 / 72 含 form / 69 域名 / 64 校名 | 我写探针从真实源码实算：**73 / 72 / 70 / 65** | **73、72 采信；69、64 需补证**（§2） |
| 已更正 `竞品差距与改进方案-2026-09-11.md` | 确有未提交改动 | 内容采信，**程序违规**（§4） |

---

## 1. 独立复现证据（命令 + 观察）

```bash
# Edge 版本（核 148.0.3967.54）
powershell -c "(Get-Item 'C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe').VersionInfo.FileVersion"
# => 148.0.3967.54   精确吻合

# 浏览器 E2E 独立复跑
PW_HEADLESS=1 npm run test:e2e
# => Playwright 性能采样：北科大 14 项成果连续填写 1301ms
#    Playwright 性能采样：合工大基本信息一键填充 81ms
#    Playwright 本地扩展测试全部通过 ✅      E2E_EXIT=0
# （与 GPT 报的 1348ms / 67ms 同项不同值 —— 计时抖动，反证是真实运行而非抄录）

# 竞品目录完整性（RD-1）
ls -la "/d/巨能填系统-1.8.9/worker/browser_worker/"
# => package-lock.json Jul 29 10:37 / package.json Aug 1 04:23 / node_modules Sep 2 13:07 / . Sep 2 13:07
#    全部早于本日 → 未落盘

# 覆盖数（自备探针）
node docs/analysis/v10-review-2026-09-11/probe-adapter-counts.cjs
```

---

## 2. 覆盖数定案：73 / 72 与 GPT 一致；69 / 64 与实测差 1

**我的探针**（`docs/analysis/v10-review-2026-09-11/probe-adapter-counts.cjs`，esbuild 打包真实 `src/core/adapter-packages.ts`，`external:['esbuild','jsdom','playwright']` —— 沿用 F03 教训）：

```
包总数                : 73
含 role=form 的包     : 72
平台通配包(hosts 含 *) : 3
具体填报域名(去 * )    : 70
去重非平台校名        : 65
role 取值             : crawl-only, form, shell, upload
按 family 分布        : {"blue":20,"other":27,"minimal":4,"jingzhi":15,"cover":7}
```

**定案**：

1. **"73 包"正确，"54 包"错误** —— 另一窗口的 54 = 39 显式 + 15 蓝系，**漏计同数组中 9 个复古生成包 + 10 个通用映射生成包**（39+15+9+10 = 73）。GPT 这一纠错成立，我采信并把记忆里"73 作废勿引"的错结论**已改正**。
2. **"72 含 form"精确一致**（差集唯一：`nuaa-ssxly` 仅 crawl-only）。
3. **"69 域名 / 64 校名" vs 实测 70 / 65 —— 各差 1。** 我逐条 dump 过：非通配包恰 70 个（73 − 3 通配），域名无重复；去重非平台校名 65 个（重复来自同校两包，如南理工 tmybm/xly、厦大 ssyjsbm/dxsxly、地大 tms/xly）。**差 1 只能来自 GPT 侧一条未公开的额外排除规则**（候选：把 `zhaosheng.eol.cn` / `*.360eol.com` 视为平台域名，或把科研院所校名排除）。
   **要求**：GPT **补发探针命令与脚本**；在补发前，任务书 §1 该两个数字以审查者实测 **70 / 65** 为准。理由不是"我更大"，而是 **P 系列纪律要求数字可复跑**——不可复跑的数字不得进冻结文档。

---

## 3. 纪律收紧建议：RD-1 应禁止"在竞品目录内执行包脚本"

**事实**：GPT 在 `D:\巨能填系统-1.8.9\worker\browser_worker` 实跑了 `npm test`（缺 `test/adapter_operation_matrix.test.js` → exit 1）。

**我的核查结论**：**未落盘，RD-1 实体未被破坏**（§1 证据）。

**但这是擦边**：`npm test` 是**执行该包的脚本**，可能写 `node_modules/.cache`、lockfile 或产物目录。本轮侥幸未写，不代表下次不写。RD-1 现文只说"不改动/不复制"，未覆盖"执行"。
**建议（非阻塞）**：RD-1 增一句 —— **"禁止在 `D:\巨能填系统-1.8.9` 内执行任何包脚本（`npm`/`npx`/`python -m`/`*.exe`）；对该目录只允许文件系统只读操作（读/列目录/哈希）；任何需要在其中执行的动作须先经用户与审查者双批"**，并把本轮这次擦边记入账本"剩余限制"。

---

## 4. 程序违规（阻塞级流程项）：GPT 写了执行者的账本与 F00 交付物

**事实**：GPT 的改动进入了两个**它不该写**的文件（未提交，`git diff --stat` 可见）：

| 文件 | 改动 | 归属 |
|---|---|---|
| `docs/dev-notes/v10-execution-ledger.md` | +10 行，追加 `## L-GPT-AUDIT` 条目 | **执行者的账本**（RD-7 单写者、任务书 §1 账本条款） |
| `docs/analysis/竞品差距与改进方案-2026-09-11.md` | 4 行 ±，覆盖数纠错 | **F00 交付物**（已随 F00 冻结的产物） |

**内容我认可**（§2 已核），**程序不认可**：

- 任务书 §1 只定义三种角色（执行者 / 审查者 / 用户），**GPT 不在角色表内**；RD-7 明确"单写者规则"。
- 后果不是"文件被写坏"，而是**审计链不再干净**：账本是"谁在何时因何改了什么"的唯一权威，第三方直写后，后续任何"这段是谁写的"追问都无法回答。
- 附带发现：**三个审查侧产物全部未跟踪**（`?? docs/analysis/v10-review-2026-09-11/` 下的 GPT 审计、我的复审、我的启动提示词）。而由于 **B-1**（哈希工具对基线外新增文件零覆盖），**这些文件对哈希审计完全不可见** —— 这是我上一轮所报 B-1 漏洞的一个**真实在野实例**，请执行者在补 B-1 时把它一并作为验收用例。

**处置（二选一，由用户裁定）**：

- **(a) 保持单写者**：把 GPT 对账本/F00 文档的改动**回退**，其结论由**执行者**以自己名义追加一条账本条目（标注"来源：GPT 审计，已由执行者复算 73/72 一致"）。
- **(b) 正式扩角色**：任务书 §1 增第四角色 —— **"第三方审计员（GPT）：只读生产代码与任务书；产出仅落 `docs/analysis/v10-review-*/`；不得写账本、任务书、F00/F04 交付物；其发现由执行者转录进账本"**，随后按 (a) 的方式转录。

我倾向 **(b)**：既保留 GPT 的贡献，又把写盘权收回到单写者手里。

---

## 5. 我接受 GPT 对 oracle 协议的改进

GPT 指出：我上一轮定的五字段协议 `{夹具页, 控件定位, 期望字面值, precision, 依据}` **缺"档案源值 → 独立转换规则 → 期望字面值"这条证据链**。

**我接受，这是对的。** 缺了转换规则列，审计者无法独立重演推导。协议修订为六字段：

```
{ 夹具页, 控件定位, 档案源值(profile 路径+原值), 独立转换规则(如 2022-09 --maxlength=8--> 202209),
  期望字面值, precision, 依据(外部可核物: 夹具 DOM name/id/label、页面 maxlength/pattern、码表条目) }
```

此修订须由**执行者**写进任务书 §3-F01 的 oracle 条款（我不写任务书，RD-7）。

---

## 6. F03/F04 状态不变

`HEAD = a886a3f`，**无新提交**，B-1/B-2 **未修**。因此：

- `v10-hashes check` 仍应报 3 DRIFT + 1 MISSING（预期审计态）；
- **仍不得 `update` 签名**；
- **F01 不得开工**（任务书卡序与我的启动提示词一致）。

本轮 GPT 的三项工作（Edge / 竞品不可测 / D1–D3 未启动）**都不属于 F03/F04 的解冻条件**，不改变闸门。

**可执行性最强的一条**：B-1 现在有了在野实例（§4 末），补它时直接拿"审查目录下三个未跟踪文件"当验收输入即可，比人造探针更硬。

---

## 7. 我的产出位置声明

按 RD-7，**我不写账本、不写任务书、不改 F00 交付物**——本文件即我的全部产出，落在审查者目录。GPT 的产出亦应限定在同目录（§4 处置项）。

---

## 8. 结案（第二轮复核，2026-09-11）

### 8.1 数字争议：不是谁算错，是两个分母 —— 已结案

我方探针与 GPT 探针是**两套独立实现**（我：写 `os.tmpdir()` + 动态 `import`；GPT：`write:false` + `new Function` 内存执行），六个数字**全部互现一致**：

```
口径一 · 库存（全部非通配包）    : 73 包 / 72 含 form / 3 通配 / 70 域名 / 65 校名
口径二 · 严格 form-fill          : 69 域名 / 64 校名
唯一差项                        : nuaa-ssxly | 南京航空航天大学 | yzsbm.nuaa.edu.cn | roles=crawl-only
```

**我撤回上一轮"69/64 与实测差 1、须补证"的质疑**：GPT 侧并无未公开的排除规则，是我把"库存口径"当成了唯一口径，而"具体填报域名"这个旧叫法本身含糊。**两套数字都对，须双口径并呈**：库存 70/65 是"有多少入口"，form-fill 69/64 是"其中多少真能填"。范围结论不变：**大体持平、无一方碾压**。

**定稿写法**（任务书/文档引用一律用此表述）：
- 库存口径：73 个运行时包对象；72 个含 `form`；70 个含 `crawl-only` 的具体域名；65 个含 `crawl-only` 的去重非平台校名。分解 `39 + 15 + 9 + 10 = 73`。
- form-fill 口径：69 个可填写具体域名；64 个可填写非平台校名。
- 作废项：**"108 域名 / 102 校"**（`schools.ts` 名称表误计为覆盖）与**"54 包 / 约 50 校"**（漏计 9 复古 + 10 通用映射生成包）——两者均不得再引。
- "73 包"正确，不得与上述两个错项一并作废。

### 8.2 GPT 的两项纠正与一项加分

| 项 | 判定 |
|---|---|
| 处置方案 (a) 撤销对账本与 F00 交付物的改动 | **已核**：两文件 `git diff` 均为空，回退精确干净 ✓ |
| 补交不留临时文件的只读探针 | **已核**：`write:false` + `new Function`，确不落盘；且内置"计数漂移即 throw"的自校验，是优于我的写法 ✓ |
| RD-7 自我纠正 + 承诺不在竞品目录执行脚本 | **已核**：报告 §7 有记录；竞品目录时间戳仍全停在 Sep 2 ✓ |

**额外加分（须记录）**：GPT 诚实披露 computer-use 连接器连续两次失败（`nodeRepl.fetch request failed`），并**明确声明没有把 `npm run test:e2e` 冒充成 `edge://extensions` 手工加载**——即它把"真实 Edge headful E2E 通过"与"持久安装到用户日常 profile"两件事分得很清。这正是本工作流最看重的诚实性：**未运行就说未运行**。

### 8.3 审查者自查（我也犯了同类错）

GPT 指出我的探针在审查目录留下未跟踪的 `probe-adapter-counts.bundle.cjs`。**属实，且这正是我上一轮所报 B-1 的那个漏洞的在野实例。** 已处置：
- 删除该 55KB bundle；
- 改写探针为写 `os.tmpdir()` 并在 `finally` 中清理，复跑确认**不再留任何产物**（含 tmp 无残留）。

教训与 B-1 同源：**审查者目录也是仓库的一部分，探针不该污染它**。GPT 的 `write:false` 内存执行是更好的做法，我已采纳同类思路。

### 8.4 B-1 的在野实例清单（补 B-1 时直接用作验收输入）

`docs/analysis/v10-review-2026-09-11/` 下当前**全部 7 个文件对哈希基线均不可见**（6 个 `??` 未跟踪 + 1 个已删除的 bundle 从未被记录）。补 B-1 时，直接以"该目录的文件必须逐个出现在某张卡的 touch-list 内"为验收断言即可，比人造探针更硬。

### 8.5 闸门复核：未变

`HEAD` 仍为 `a886a3f`，**无新提交**；**B-1 / B-2 未修**。故：

```
仍不得 v10-hashes update 签名；F01 不得开工。
解冻条件仍为：B-1(哈希工具新增文件覆盖 + .gitattributes 入签 + 负向自检)
              + B-2(e2e 因果对照 + 真负向自检)
              + N-1..N-4(记账与流程)
   → 我复跑 npm test / check:adapters / test:regression / PW_HEADLESS=1 test:e2e / v10-hashes check
   → 出具"F03/F04 过审" → 才可 update → 才进 F01
```

本轮 GPT 的三项工作（Edge / 竞品不可测 / D1–D3 未启动）与数字定案，**均不属于解冻条件**，不改变闸门。

### 8.6 待用户裁定（仍未决）

§4 的处置二选一，GPT 已按 **(a)** 执行完毕。**建议顺带采纳 (b) 的一半**：任务书 §1 增设第四角色"第三方审计员（GPT）：只读；产出仅落 `docs/analysis/v10-review-*/`；不得写账本/任务书/冻结交付物；发现由执行者转录进账本"——这样下次 GPT 的审计结论有正式落点，不必再由执行者事后追认。此条须由**执行者**写入任务书（RD-7）。
