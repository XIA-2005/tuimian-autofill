# CONTEXT — tuimian-autofill 项目评审 + 竞品对比

## 术语

- **tuimian-autofill**：本项目，纯浏览器扩展，v2.0.0，Edge/Chrome 双兼容。
- **巨能填系统**：竞品，Qt 桌面 + Python 桥接层 + 浏览器扩展三重架构，v1.8.9，本地服务 127.0.0.1:18765。
- **原子表（Atomic Table）**：V2 引入的细粒度结构化数据，8 类（论文/专利/项目/竞赛/荣誉/实习/实践/学生工作）。
- **字段锁定（Field Lock）**：档案值被人工填写后自动锁定，V2 引入防爬取覆盖机制。
- **字段推导（Derive）**：从原始档案值计算衍生字段（rankPercent/cetSummary/studyDuration）。
- **平台适配器（Adapter）**：按 URL 指纹匹配学校系统，注入专属规则和控件驱动。
- **蓝色平台（Blue Platform）**：基于统一蓝色皮肤封装的 10+ 所高校报名系统，共用 blue-flat 样式层。
- **弹窗选择框（Popup Picker）**：隐藏 input + "选择" 按钮触发弹窗的学校/专业/省市级联选择器。
- **组件下拉（Component Dropdown）**：无原生 select 的 JS 组件（jqx/Layui/Ant/Kendo）识别与点选。
- **TDD 铁律**：项目开发必须遵守的测试先行纪律（详见决策 1）。

## 决策

### 决策 1：TDD 工作流铁律（严格模式 R5）
**结论：所有 AI 生成代码必须有先红的测试**

**铁律 1（双会话隔离）**：
- 红阶段（写测试）与绿阶段（写实现）必须在不同 AI 会话中完成。
- 红阶段提示词头部带 `[TDD-PHASE: red]` 标记。
- 绿阶段提示词头部带 `[TDD-PHASE: green]` 标记。
- 绿阶段 AI 不准看实现文件，只能看测试文件。

**铁律 2（红必须红）**：
- 红阶段写完测试必须运行一次，**测试必须 FAIL**（因无实现）。
- 测试一开始就是绿的 = AI 作弊，整轮打回。

**铁律 3（业务规则优先）**：
- 断言用业务不变量（正则/范围/关系），不用示例值。
- 反例：`expect(name).toBe("张三")` —— AI 知道这个值。
- 正例：`expect(idCard).toMatch(/^\d{17}[\dXx]$/)` —— 这是规则。

**铁律 4（三类覆盖）**：
- 正常路径 / 异常路径 / 边界值 三类各 ≥ 1 case。
- 单 case 测试集必须打回补充。

**铁律 5（提示词工程化）**：
- 项目根目录 `.cursorrules` 写入上述 4 条铁律。
- 提示词必须 6 段式：阶段标记 / 项目上下文 / 业务规则 / 边界清单 / 禁止条款 / 交付物。
- `bridge/scripts/tdd-prompt.py` 模板脚本生成标准化提示词。

**不选 R1 的原因**：人写测试、 AI 写实现太理想化，AI 集成 IDE 时代不切实际。
**不选 R4 的原因**：人 review AI 写的测试草稿，治标不治本——AI 写测试时仍可看到实现上下文。
**R5 的硬约束**：CI 流水线加 `npm run test:must-be-red` 检查每个 PR 配套测试是否真红过（git history 里能查到的最早期 commit 测试为红）。

### 决策 2：MVP 与 TDD 工具链的协调
**结论：T3 — MVP 不变，TDD 工具链作为 v2.5 增量**

- **MVP R2 维持原计划**：4-5 周，Go 二进制 + 西电单学校闭环。
- **TDD 工具链 v2.5 增量**：1-2 周，与 MVP 同期交付。
  - `.cursorrules` 文件
  - `bridge/scripts/tdd-prompt.py` 模板脚本
  - `npm run test:must-be-red` CI 检查
  - 6 段式提示词示例库
- **不选 T1 的原因**：停 MVP 2 周损失节奏。
- **不选 T2 的原因**：MVP 模块与 TDD 工具链并发时容易顾此失彼，TDD 工具不完善时易绕过铁律。

### 决策 3：方向 B 最优先解决的填充深度问题
**结论：B3 — 漏填高亮实现**

- **目标**：实现 README 描述的「绿色=已填，红色=需人工，黄色=档案未填」三色高亮。
- **位置**：`src/content/panel.ts` 与 `src/core/filler.ts`。
- **不选 B1 的原因**：组件下拉覆盖虽重要，但每种框架适配需独立 TDD，跨度长；可作为 B3 之后的 B1+。
- **不选 B2 的原因**：省份选择器依赖 `regions.ts` 数据完善 + 弹窗选择器内核，与 B3 独立但工作量更大。
- **不选 B4 的原因**：蓝色平台适配需要逐校真实环境测试，依赖用户登录态，TDD 难以孤立执行。

### 决策 4：B3 漏填高亮的 TDD 拆分策略
**结论：U1 — 两个独立 PR**

- **PR 1（核心逻辑）**：
  - 文件：`src/core/highlight.ts` + `src/core/highlight.test.ts`
  - 红阶段提示词 → AI 写测试 → 必须 FAIL
  - 绿阶段提示词 → AI 写核心函数 → 必须 PASS
  - 该 PR 是纯函数，100% TDD 覆盖。
- **PR 2（UI 渲染）**：
  - 文件：`src/content/panel.ts` + `src/content/panel.test.ts`
  - 红阶段提示词 → AI 用 jsdom 写测试（DOM 操作）→ 必须 FAIL
  - 绿阶段提示词 → AI 调用 PR 1 的核心函数 + 加 CSS 类 → 必须 PASS
  - 该 PR 依赖 PR 1 已合并。
- **不选 U2 的原因**：一个 PR 包含两段 TDD，AI 上下文压力大；U1 拆开让每轮 TDD 闭环更清晰。

## 竞品对比维度（待盘问 3 个方向）

- 方向 A：架构差距（本地服务层、跨域能力、OCR）
- 方向 B：填充深度（学校适配广度、组件下拉覆盖、弹窗选择器完整度）
- 方向 C：用户工程化（Onboarding 流程、状态可视化、提示词工作流）

---

## 薄弱点（已确认存在）

1. 蓝色平台只做浅层识别。
2. 原子表 UI 不完整。
3. 适配器覆盖广度有余、深度不足。
4. 漏填高亮未实现。
5. 省份选择器缺失。
6. 填充进度感知弱。
7. 组件下拉覆盖不完整。
8. 学校导航不完整。
9. 无 TDD 铁律的工程化（决策 1 已修复）。
